# CLAUDE.md — AwayTerminal

Windows 原生終端機模擬器。**C# WPF (.NET 9) 外殼 + WebView2 內嵌 xterm.js（終端機渲染）+ ConPTY / 內建 Telnet / 序列埠**。
使用者：awaysu@gmail.com（偏好繁體中文回覆）。名稱：中英文皆為「**AwayTerminal**」（`Loc.T("app.name")`；原中文名「艾維終端機」已全面改為 AwayTerminal）。
GitHub：原始碼 https://github.com/awaysu/AwayTerminal（public）。**安裝檔下載站＝ https://www.awaysu.cc/software/awayterminal**（自架，原始碼在 GitHub `awaysu/software-web`；舊記載的 `awaysu/Download` repo 不存在）。

## 發佈流程（1.1.0 起，2026-08-28 實跑一遍）
1. 版本：`AwayTerminal.csproj` `<Version>` 與 `installer/installer.iss` `AppVersion` 同步改；CLAUDE.md／README 更新；commit + push main。
2. 安裝檔：`rm -rf bin/publish` → `dotnet publish AwayTerminal.csproj -c Release -r win-x64 --self-contained false -o bin/publish` → `sign.ps1 bin\publish\AwayTerminal.exe` → `MSYS_NO_PATHCONV=1 "$LOCALAPPDATA/Programs/Inno Setup 6/ISCC.exe" /Q installer/installer.iss`（Git Bash 下 `/Q` 會被轉成路徑，一定要加 `MSYS_NO_PATHCONV=1`）→ `sign.ps1 installer\dist\AwayTerminal-Setup-<ver>.exe`。`installer/` 需有 `MicrosoftEdgeWebview2Setup.exe`（https://go.microsoft.com/fwlink/p/?LinkId=2124703）與 `windowsdesktop-runtime-win-x64.exe`（https://aka.ms/dotnet/9.0/windowsdesktop-runtime-win-x64.exe），皆 gitignore、缺了去抓。
3. 免安裝 zip（站上 kind=archive）：`dotnet publish … --self-contained true -o bin/publish-portable` → 簽 exe → 複製成 `bin/zipstage/AwayTerminal-<ver>/` → `Compress-Archive` 成 `installer/dist/AwayTerminal-<ver>-portable.zip`（**頂層資料夾 `AwayTerminal-<ver>\`**，與 1.0.44 相同；約 74MB，站上單檔上限 100MB）。
4. 上傳（API＝`https://www.awaysu.cc/software/api.php`；規格＝`awaysu/software-web` 的 `readme_for_program.txt`；密碼＝網站後台登入密碼，存在 `D:\Awaysu\web_info.txt` **第 5 行「網頁登入密碼為xxxxx」、沒有冒號**，取法 `sed -n 5p … | tr -d '\r\n' | grep -oE '[A-Za-z0-9]+$'`（用 `[:：]` 切會切進「碼」的 UTF-8 位元組），**只走 `X-Api-Password` header、不進 log／commit**；1.1.1 於 2026-08-29 照此流程實跑一遍全通）：
   - 補歷史：`curl -F app=awayterminal -F version=<v> -F date=<yyyy-mm-dd> -F "changelog=<notes.txt" "…/api.php?action=set_changelog"`（每版一則，舊→新依序呼叫；同版本會取代）。
   - 上傳：`curl -H "X-Api-Password: …" -F app=awayterminal -F version=<ver> -F platform=windows -F "changelog=<notes.txt" -F sha256=<hex> -F "file=@<exe>" "…/api.php?action=upload"`；zip 同樣一次（同 platform 同副檔名會自動取代舊項目、id 不變）。
   - 驗證：`GET api.php?action=check_update&app=awayterminal&platform=windows&version=<舊版>` 看 `latest_version`／`downloads[].sha256`／`changelog`。
   更新說明格式：每行 `- …`，使用者看得懂的功能描述（站上會自動加 `v<ver> (日期)` 標題）。

## 建置 / 執行 / 測試
```powershell
dotnet build                                     # 建置（會自動簽章 exe，見「數位簽章」）
.\bin\Debug\net9.0-windows\AwayTerminal.exe      # 執行
```
- GUI 程式，**無法在此環境互動測試**。驗證：`dotnet build` 0 錯誤 + 冒煙啟動
  （`Start-Process` 後檢查子行程 msedgewebview2.exe / powershell.exe / conhost.exe）。
- 從工具環境啟動 exe 會被沙箱擋 → `dangerouslyDisableSandbox: true`。
- 重建前先 `Get-Process AwayTerminal | Stop-Process -Force`，否則 exe 鎖住 build 失敗（MSB3027）。
- UI 疑難時請使用者截圖，用 `System.Drawing` **逐像素取樣**分析（曾靠此找到 xterm 黑帶 #000）。

## 架構
- **WPF 原生 chrome**：工具列（上方橫排、圖上字下、寬 72px；1.0.46 移除右側直欄模式）、分頁列表框（**在終端機右側、由上而下**，1.1.0；左緣 `GridSplitter` 拖寬、右上 ▼/▲ 顯示隱藏、寬度與顯示記於設定；1.0.x 為視窗底部橫排分頁列）、各對話框、右鍵選單。
- **單一 WebView2** 載入 `web/index.html`；`terminal.js` 管理多個 xterm 實例；分頁模式只顯示 active，分割/分欄用 CSS grid 全顯。
- **C# 每分頁一個 `ITerminalSession`**：`ConPtySession`(PowerShell / claude.exe / ssh.exe / adb.exe / 自訂 exe)、`TelnetSession`(TCP+IAC)、`SerialSession`(COM，專用 blocking-read 執行緒)。
- **`TermKind`**：PowerShell / Ssh / Telnet / Com / Adb / Claude / Custom。狀態燈：PowerShell=子行程樹+近 1.5s 輸出；Claude/Custom=近 1.2s 輸出；其餘（遠端/COM）=近 0.5s 輸出。

### C# ↔ JS 訊息協定（字串；分隔字元 US = ``）
- JS→C#：`i{id}{US}{text}`(輸入)、`r{id}{US}{cols},{rows}`(尺寸)、`a{id}{US}{kind}{US}{text}`(查詢回覆)、
  `p{id}`(點選 pane)、`k{id1,id2,...}`(拖曳後新順序)、`z{size}`(Ctrl+滾輪縮放後字級)、`ready`
- C#→JS：`o{id}{US}{base64}`(輸出)、`n{id}{US}{title}[{US}{flags}]`(建立；flags `c`=claude 貼上走 ESC+CR)、`t{id}{US}{title}`(改名)、
  `s{id}`/`x{id}`/`c{id}`/`f{id}`(顯示/關閉/清除/聚焦)、`L{tab|split|columns}`(模式)、
  `S{id}{US}{up|down|top|bottom}`(捲動檢視＝工具列「翻頁」；只動 xterm 視窗、不送輸入)、
  `q{id}{US}{sel|selpaste|all|text|file}`(查詢文字；selpaste=同 sel 回選取文字、C# 收到後再貼回原分頁；text=遠端用、渲染後純文字最後 400 行；file=整個 buffer 純文字、回覆後存檔；**save=關閉程式時取 scrollback 序列化含顏色供下次恢復，1.0.45**)、`T{json}`(全域字型/顏色；1.0.43 起多帶 `imeQuietMs`、1.0.45 多帶 `restoreLines`)、`P{id}{US}{fg}{US}{bg}`(單一分頁配色；空=回設定預設)、`A{id}`(全選)、`F`(開 Ctrl+F 搜尋列)、`v{id}{US}{base64}`(貼上→JS `doPaste`：一般=`term.paste()`、claude=ESC+CR)、**`b{id}{US}{base64 舊內容}{US}{base64 分隔行}`(1.0.45：恢復分頁／重連前把舊內容寫進 xterm、再整頁推進 scrollback 並把游標歸左上，之後才啟動新 session；兩欄皆空＝只推。JS 端等 pane fitted 才寫、期間 `o` 輸出扣住，見 `terminal.js applyRestore`)**
- 改協定時 **C# 與 `web/terminal.js` 必須同步**。

## 關鍵檔案
| 檔 | 作用 |
|----|------|
| `MainWindow.xaml(.cs)` | 主視窗：工具列/右側分頁列表框（1.1.0）/狀態輪詢/工作列圓點/New 下拉/紀錄/自訂連線/輸入文字（ComposeDialog）/login-as/恢復分頁 |
| `App.xaml.cs` | 啟動時清理環境變數（見「踩雷」）＋ TERM/COLORTERM |
| `ConPty/` | ConPTY P/Invoke（含 TerminateProcess、`GracefulExitBytes` 優雅關閉；預設 Ctrl+C ×3）|
| `Sessions/` | ITerminalSession + Telnet/Serial（Serial 用 blocking-read 執行緒，非 DataReceived）|
| `Dialogs/` | CustomConnDialog(自訂連線)、PromptDialog(常用字串+群組)、RemoteDialog(遠端設定)、SettingsDialog、ConnectDialog、ComDialog、ExitDialog、LogDialog、InputDialog、ComposeDialog(工具列「輸入文字」先打字再送出；1.0.30 建立、1.0.46 併入常用字串列)… |
| `Services/TelegramRemote.cs` | Telegram 遠端控制（自寫 long polling、指令解析、`IRemoteHost` 介面、`TidyForPhone` 雜訊過濾）|
| `Services/ShellIntegration.cs` `Services/IpcPipe.cs` | 1.0.45：檔案總管資料夾右鍵「用 AwayTerminal 開啟」（HKCU `Directory\shell`／`Directory\Background\shell`，每次啟動依 `ExplorerMenu` 重寫、路徑指向目前 exe）＋實例間具名管線 `AwayTerminal.OpenDir`（`--open-dir` 轉交既有實例、本行程結束）|
| `Models/TerminalTab.cs` | 分頁模型（狀態、尺寸、`StartUtc`+已啟動時間 tooltip、PendingCommand、LoginBuffer、Restore）|
| `Services/AppSettings.cs` | 設定（`%LOCALAPPDATA%\AwayTerminal\settings.json`）：CustomConns、History、Prompts(含 Group)、字型/顏色、SavedTabs(1.0.45 起含 `BufferFile`→`%LOCALAPPDATA%\AwayTerminal\restore\tabN.txt`)、`RestoreBufferLines`(恢復分頁保留的 scrollback 行數，預設 2000、只在 JSON 調)、`ExplorerMenu`… |
| `Localization/Loc.cs` | 中/英字串表 + `LocProxy`（XAML 綁定動態換語言）|
| `Logging/` `Macros/` | log 記錄（時間戳 `[yy-MM-dd HH:mm:ss]`）/ TTL 巨集直譯器 |
| `web/` | 前端（`terminal.js` + `vendor/` xterm）；`icon/` 工具列與圖示 PNG（WPF Resource）|
| `LICENSE` / `THIRD-PARTY-NOTICES.md` | MIT（`Copyright (c) 2026 Chih-Wei Su (Awaysu)` —— 本名 2026-08 補上，Certum 開源憑證要能從公開紀錄把證件本名連到專案）＋第三方聲明。**由 csproj 的 `Content` 複製進發佈輸出**，安裝檔與 MSIX 兩邊自動含入（MIT 要求「所有副本」都附聲明，只放 repo 不算數）|
| `installer/` | Inno Setup 安裝腳本 `installer.iss` + 公開憑證 `AwayTerminal.cer` + 使用者選擇性信任腳本 `trust-publisher.ps1`（見「數位簽章 / 安裝檔」）|
| `msix/` | MSIX 打包（上架 Microsoft Store 用）：`AppxManifest.xml` + `build-msix.ps1` + `make-images.ps1` + `Images/`；`layout/` `out/` 為產出，已 gitignore |
| `app.ico` | 由 `icon/app-icon.png` 產生（PNG 內嵌 ICO）|

## 慣例
- **視窗標題**：`AwayTerminal - 目前路徑`（v1.0.6，1.0.9 擴充）。路徑由**提示字元行解析**（`q…cwd` 查游標所在提示行 → `CwdRes` 六組 regex：PowerShell / cmd / bash-zsh / RHEL `[user@host ~]#` / Android adb `host:/path $` / fish `user@host /path>`），狀態輪詢每 0.6s 更新；解析不到就保留上次值（打字中不閃動）。**沒有提示行的分頁（claude/自訂）用 `TerminalTab.WorkDir`（啟動目錄）墊底**，切分頁以 WorkDir 起始、無分頁時只顯示程式名。**註：不能讀行程 PEB 的 CWD**——PowerShell 的 `Set-Location` 不會同步行程工作目錄，讀到的永遠是啟動目錄。
- **版本**：每交付一次 `<Version>` +0.0.1（「關於」顯示）。目前 **1.1.1**（1.1.1=**SSH 帶 `-o SendEnv=CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN`（2026-08-28）**：使用者回報「SSH 連線後執行 claude，回覆後之前的對話不見了」＝遠端 claude 走 alternate screen；本機是 `App.OnStartup` 設的環境變數、SSH 不會自動帶到遠端。`SshCommand()` 加 SendEnv（四個 SSH 啟動點共用）；**遠端 sshd 要 `AcceptEnv` 允許此名才收**（Debian/Ubuntu 預設只收 LANG/LC_*），沒允許就靜默忽略、無副作用。**2026-08-29 實機定案（使用者 Mac mini 192.168.50.118、claude 2.1.251）**：① macOS sshd 預設 `AcceptEnv LANG LC_*`＝SendEnv 到不了，遠端要在 **`~/.zshrc`** export（Mac 預設 zsh；使用者原本放 `~/.bashrc` 沒用）；② claude 2.1.251 的「fullscreen trial／downsell graduation」會在 `tui` 未設時**自己把 `"tui":"fullscreen"` 寫進 `~/.claude/settings.json`**（使用者的檔當天 08:21 被寫入＝「現在更慘」），已改 `"tui":"default"`（合法值只有 default/fullscreen；env var 優先於 tui，`/tui default` 也可）；③ 機制＝**Win10 ConPTY 不轉送 alt-screen**（見踩雷）。驗證工具 scratchpad `sshprobe`（ssh.exe 跑 ConPTY、腳本化登入→cd→claude→提問→存 raw）＋`replay.js`（headless xterm 數 baseY）：修前 1049h=0 次／scrollback 0 行、修後 scrollback 42 行含問題與回答前半；1.1.0=**分頁列改為右側列表框（使用者要求的「小改版」，2026-08-28）**：底部橫排分頁列與右下「▲ 全部分頁」清單（`TabList_Click`）移除；主區改為 Grid 三欄＝終端機 `*`（MinWidth 200）｜`GridSplitter` 5px（`ResizeBehavior=PreviousAndNext`）｜`TabPanelCol`（預設 220px、最窄 120）內 `ScrollViewer`＋`TabStrip`（`StackPanel Vertical`；每列 DockPanel：狀態圓點／log／巨集／標題（CharacterEllipsis）／右側 ✕，右鍵選單照舊、tooltip 照舊）；作用中＝黃框＋#1E1E1E 底、非作用中灰底無框，`TerminalTab.TabBorderThickness`/`TabMargin` 刪除；右上 `TabPanelToggle`（沿用舊快速輸入抽屜的 ▼/▲ 樣式與位置：root Grid 右上 Margin 0,40,8,0）→ `TabPanelToggle_Click` 切 `AppSettings.TabPanelVisible`（預設顯示），`ApplyTabPanel`（OnLoaded 也呼叫）隱藏時欄寬 0＋MinWidth 0＋列表與拖曳區 Collapsed、顯示時欄寬回 `TabPanelWidth`；`TabSplitter_DragCompleted` 存 `TabPanelWidth`；Loc `tip.tabList`→`tip.tabPanel`；1.0.46=**輸入介面整合（使用者要求，2026-08-28）**：① 分頁列最左「…」與工具列右端 ▼ 快速輸入抽屜移除（`QuickPanel`／`QuickToggle`／`Quick*` 六個方法／`_quickItems`／`SizeChanged` 調寬），改為工具列「**輸入文字**」鈕 `BtnCompose`（在「複製」左邊；新圖示 `icon/compose.png`＝文字框＋I-beam，用 System.Drawing 依家族風格產生：128×128、填 #CDCDCD、描邊 #303030、內線 #7A7A7A）開 `ComposeDialog`；② `ComposeDialog` 上方新增常用字串列：群組／標題下拉＋「插入」（插進文字框游標處）＋「直接送出」（不經文字框、依字串 `SendEnter` 立即 `SendSnippet`，視窗留著可連送、按鈕短暫「✓ 已送出」），建構式改 `(sendEnter, IReadOnlyList<PromptItem>, Action<string,bool> sendNow)`，下拉列下方唯讀 `PreviewBox` 顯示所選內容、與輸入框 2:3 分高（輸入框＝原本 3/5），上次群組／標題記 static；③ 移除功能列上／右切換（`BtnTaskbar`、`TaskbarToggle_Click`、`ApplyToolbarPosition`、`ToolBtnEx.cs`、`AppSettings.ToolbarPosition`、`taskbar-change.png`、Loc `tb.taskbar`/`tip.taskbar`/`quick.tip`/`compose.tip`；新增 `tb.compose`/`tip.compose`/`compose.insert`/`compose.sendNow`/`compose.sent`），三個下拉選單 placement 固定 Bottom，`MainWindow.xaml` 終端機不再包在兩欄 Grid 裡。驗證（UIA 自動化、與安裝版 1.0.45 並存執行）：工具列排列、紀錄開分頁→輸入文字→插入→直接送出（字串進 PowerShell 輸入列）→返回→關閉全通；紀錄下拉的 MenuItem 是圖示＋文字複合 header、UIA Name 為空，要用 Text 子元素往上找 MenuItem（`uia.ps1 -MenuLike`）；1.0.45=**使用者五項回饋（2026-08-28）**：① **注音開「獨立小視窗」時仍重複二次**：讀 vendored xterm 6.0.0 找到 JS 層重複路徑——IME 以浮動視窗組字時提交走 textarea `input(inputType=insertText)`，`_inputEvent` 在 keyup 後（`_keyDownSeen=false`）直接 triggerDataEvent 一次、`compositionend` 排的 setTimeout（`_finalizeComposition`）再從 textarea.value 送一次，而 `_dataAlreadySent` 只有 `_handleAnyTextareaChanges` 會設，兩份都出去；inline 組字走 insertCompositionText 沒這條路＝平常不發生、對上「只在小視窗出現時」。修＝`terminal.js` `sendTyped` 去重：同分頁同字串、期間**沒有任何 keydown**（`rec.keySeq`，body capture 層每個 keydown +1）、`IME_DUP_MS=100` 內再來＝丟第二份並寫 diag.log `ime-dup dropped …`；有 keydown 一律放行（連按兩次「！」／按住自動重複都有各自 keydown，不誤殺）。**無 IME 實機驗證**，之後看 diag.log 有沒有 `ime-dup` 就知道重複是 JS 層（已擋）還是 claude 端；② 半形/全形游標偏（「你好嗎」按空白變「你 嗎」）＝claude 端輸入列渲染（既有 1.0.43 結論），本版無新修，繞法仍是「…」輸入框；③ **SSH/Telnet/COM 重連保留舊訊息**：根因＝**ConPTY 新 session 第一幀必送 `ESC[2J`**（conhost XtermEngine 首次 StartPaint `_ClearScreen`），xterm ED 2 原地清可視區、不推進 scrollback → 重連後「最後一頁」憑空消失。修＝`TryReconnect` 前送 `b` 協定「推進 scrollback」（`ESC[?1049l ESC[r ESC[0m ESC[{rows};1H`＋`\r\n`×rows＋`ESC[H`：alt screen 回正常、解捲動區域、整頁推上去、游標回左上＝2J 只清到空白；游標在頂、底下全空白，之後視窗變高也不會把 scrollback 拉回）。另**沒勾自動重連**的遠端分頁結束時印黃字「[按 Enter 在此分頁重新連線]」（`term.exitedEnter`），`i` 協定在 `Session==null && Restore!=null && Kind∈Ssh/Telnet/Com` 收到 CR → `ManualReconnect`（SSH Host 沒 `user@` 回「login as:」；自動重連退避等待中按 Enter＝立刻連；手動重連失敗印紅字＋提示、不進退避迴圈）；④ **恢復分頁含 scrollback**：`OnClosingAsk` 改為確認後交 `FinishExitAsync`（`_exiting` 早設、X 再按忽略）→ `CaptureBuffersAsync` 對每個有 Restore 的分頁送 `q…save`（JS `saveBuffer`＝SerializeAddon `{scrollback: SAVE_LINES, excludeAltBuffer, excludeModes}`，`SAVE_LINES` 由 `T` 的 `restoreLines`＝`AppSettings.RestoreBufferLines` 預設 2000）最多等 2.5s → 寫 `restore\tabN.txt`（每次清目錄重寫）、`SavedTab.BufferFile`。啟動 `RestoreTabs` 逐筆 `_restoreBufferForNextTab=LoadRestoreBuffer(st)`（finally 清）→ `AddTab` 在 `n` 之後送 `b`（舊內容＋`\x1b[0m`；分隔行灰字 `term.restoredSep` 帶檔案時間）再由呼叫端啟動連線＝舊訊息在上、分隔行、新連線在下。**JS 端必須等 pane fitted（最終寬度）才寫**：在預設 80 欄寫、之後 fit 變寬時 xterm reflow 會把接回的長行從 scrollback 拉回可視區（`_reflowLargerAdjustViewport` ybase--），接著被 2J 清掉＝舊訊息消失；期間收到的 `o` 先扣在 `rec.held`、寫完依序補寫。**分頁模式隱藏分頁改與作用中分頁同步尺寸**（`refit` 用作用中 pane `proposeDimensions()` 對隱藏 term `resize`＋`sendResize`）＝隱藏分頁啟動時就 fitted、恢復立即套用（順帶：切到隱藏分頁不再先舊尺寸再重排閃一下）；4 秒都量不到（視窗最小化啟動）照寫不卡死。`x` 關閉分頁清 `restoreTimer`；⑤ **檔案總管右鍵「用 AwayTerminal 開啟」**：`Services/ShellIntegration.cs` 每次啟動與設定存檔後依 `AppSettings.ExplorerMenu`（預設開；設定視窗新增「檔案總管」群組勾選）寫／刪 HKCU `Software\Classes\Directory\shell\AwayTerminal` 與 `Directory\Background\shell\AwayTerminal`（預設值＝`shell.menuText` 隨語言、`Icon="exe",0`、`command`＝`"exe" --open-dir "%V"`，%V 兩種位置都適用＝VS Code 同款）。`App.OnStartup` `ParseOpenDir`（`--open-dir` 或單一存在的資料夾參數；修 `"C:\"` 的 `\"` 被 C 執行期吃成 `C:"`）→ `IpcPipe.TrySend("open-dir\t路徑")`（具名管線 `AwayTerminal.OpenDir`、`CurrentUserOnly`、700ms）成功就 `Environment.Exit(0)` 不開第二個視窗；連不上才正常啟動、`App.PendingOpenDir` 在 `ready` 恢復分頁之後 `OpenDirFromShell`。伺服端 `IpcPipe.StartServer`（`MaxNumberOfServerInstances=1`，使用者自己多開時第二個實例建不出管線就安靜放棄）→ `HandleIpcLine` → `OpenDirFromShell`：`BringToFront`（最小化還原＋Activate＋Topmost 開關）＋`DeferUntilWebReady`＋`OpenPowerShellDirect(dir, DirTabName(dir,"PowerShell"))`（分頁名＝資料夾名）。`installer.iss` `[UninstallRun]` 用 `reg.exe delete` 清兩個 key。**MSIX 版登錄檔被虛擬化、檔案總管看不到**（需 `desktop4:FileExplorerContextMenus` COM 擴充，未做）。**驗證（2026-08-28，全程 UIA／PostMessage 自動化、未搶前景）**：IPC 轉交第二個實例 207ms 結束、第一個實例開出「Desktop」分頁；PS 分頁 60 行彩色輸出→關閉→`restore\tab1.txt` 1359B→重開後 buffer 103 行＝舊 72 行＋分隔行＋新 session；假 telnet 伺服器（127.0.0.1:2323，送 banner 2 秒後斷）→「[連線已結束] [按 Enter 在此分頁重新連線]」→ Enter → connection #2 從可視區頂端開始、#1 留在 scrollback；隱藏分頁（Desktop）切過去可見分隔行與舊內容；1.0.44=**ClaudeCode/OpenCode 分頁以資料夾命名、關於視窗加檢查更新**（`DirTabName`/`UsesDirTitle`、`UpdateChecker`）；1.0.43=**claude 分頁輸入加「靜止閘門」降低二倍/backspace 殘影**：`terminal.js` `qPush` 多一個 `gate` 旗標＋每分頁 `lastOutMs`（`o` 輸出時更新）。只 gate「容易撞 claude 重繪」的輸入（IME 整段、貼上、backspace `\x7f`/Ctrl+Backspace `\x08`）——等 claude 輸出靜止 `QUIET_MS` 才送，每筆從入列起最多等 `QUIET_MAX_MS=150`（claude 持續重繪也不卡死；退回佇列用 unshift 保序）。一般 ASCII 打字/Enter/Ctrl 鍵不 gate、即時。`QUIET_MS` **做成使用者設定**＝`AppSettings.ImeQuietMs`（預設 **20**，範圍 0~150，0=關閉閘門立即送）；設定→JS 走既有 `PostTheme` 的 `T{json}`（多帶 `imeQuietMs`，`terminal.js` 的 `T` 分支以 `typeof==="number"` 套用、允許 0）。設定視窗新增「Claude 輸入送出」群組：數字框＋「這是什麼？」超連結（`ImeHelp_Click`→`MessageBox` 顯示 `settings.imeQuietHelp` 說明）。**數值依據（實測）**：閒置時 claude 把每次輸入重繪成單一 chunk（無空檔）＝10ms 或 30ms 都即時；忙碌串流 3 秒實測空檔 >10ms 有 34 次、>30ms 有 19 次＝門檻越大越確定「畫完了」但延遲略增（延遲只在忙碌時有感）。20 取折衷。**根因確認（三方交叉）**：① vendored `web/vendor/xterm.js` 與 npm `@xterm/xterm@6.0.0` 逐位元組相同，已含官方 IME 重複修正（PR #5024＋#3191 `_dataAlreadySent`）；② `diag.log` 實錄：textarea 殘值累積成 `一二三一二三一二三`，xterm `onData` 仍每次只發一次「一二三」＝JS 層乾淨；③ ConPTY+claude 2.1.241 重播「abc一二三」「一二三abc」「行寬換行」各種 backspace，最終畫面**都正確**。故二倍與 backspace 殘影都在 **claude 端重繪**（agents 忙/建議文字在跳時最嚴重，對應上游未修 issue #22853、#12332），AwayTerminal 送出的位元組是對的；閘門是**降頻的緩解、非根治**（要 claude 修）。逐字節流的 ghost 教訓（v1.0.30→32）不適用：這裡仍整段一次送、只延後送出時機不逐字；1.0.42=**分頁忙碌狀態圓點由橘改紅**：`TerminalTab.BusyBrush` `#FF9800`→`#F44336`，與工作列彈跳球同色（使用者要求統一）。`RemoteDialog` 的 `#FF9800`＝「遠端被其他視窗占用」警告文字，與狀態無關、保留；1.0.41=**工作列 overlay 彈跳球放大**：`MakeBounceFrame` 畫布 16→40、球半徑 4→16（占比 0.5→0.8）＝觀感約 1.6~1.7 倍。**注意 overlay 徽章是 Windows 固定小尺寸（SM_CXSMICON），大點陣圖會被縮到徽章大小**——所以無法真的把球放到「原本 3 倍」（會超出徽章被裁），徽章內極限≈填滿（原本 2 倍、且幾乎無彈跳空間）；取 0.8 占比在「夠大」與「保留可見彈跳＋落地壓扁」間平衡。squash 0.15、pen 1.8 都調過確保壓扁+描邊不出畫布（scratchpad `bounce-preview.png` 對照驗證）；1.0.40=**手機一次指令收到多則→只留完成推播（實測 hi 收 3 則）**：① **移除「送出後 700ms 即時回推」**（`HandleCommand` 文字分支）——它固定延遲常抓半成品（`~\Desktop` cwd 殘影）又與完成推播重複；已送出的指令一定會經「忙→靜止 1.2s→閒」翻閒、連快答也觸發完成推播，故只留它一則；② **內容去重**：`SendLast` 自動推播時，本次內容與上次送給該分頁相同（空白正規化）且 8 秒內 → 不送（擋忙→閒連兩次／DiffNew 錨點對不上退回快照的重送；`_lastSent` 存 sig+時間，隔久再問一樣的照送）；③ 過濾 cwd 路徑碎片 `^~[\\/][^\s]*$`（`~\Desktop`）；1.0.39=**閒置離開改看「附著分頁活動」不只手機來訊**：使用者實測「在電腦上持續工作，但手機本身 10 分鐘沒動作就自動離開、之後收不到」。原 `CheckIdleAsync` 只用 `_lastUserMsgUtc`（手機來訊）。改為 idle＝距 max(`_lastUserMsgUtc`, 附著分頁 `GetTabActivityUtc`＝max(LastOutputUtc, LastInputUtc)) 的時間——在電腦上對該分頁打字或它持續輸出時不會被判閒置；另 `OnTabIdle` 附著分頁完成工作時也重置計時＋重置 9 分鐘警告旗標。新增 `IRemoteHost.GetTabActivityUtc`（Dispatcher.Invoke 取分頁時間）。真的都沒動靜（手機沒訊、分頁沒輸出/沒打字）滿 10 分鐘才靜默離開；1.0.38=**「在 App 直接打字發問，結果沒推到手機」修正**：`UpdateStatuses` 的完成推播判斷原本只用「最後按鍵距轉閒 <2.5s＝打字回顯不推」，但 App 打字送出＋AI 快答時，轉閒也在 2.5s 內＝被誤判不推（手機發問走 `SendInputToTab` 不更新 `LastInputUtc` 所以有推——正好對上使用者現象）。改用 `TerminalTab.LastSubmitUtc`（App `i` 協定收到 `\r`/`\n`、或遠端 `enter=true` 時更新）判斷「這段忙碌有沒有送出過」：有送出→忙 ≥0.8s 就推、沒送出→維持 2.5s+3s 抑制打字噪音（送出時間容許早忙碌起點 2 秒）。見「主要功能行為→推播噪音修正」；1.0.37=**修表格攤平回歸＋歡迎框雜訊（實測回報「打 hi 蹦一堆」）**：1.0.34 的表格攤平把 claude 的**歡迎/「What's new」changelog 框**（無邊框、中間一條 `│` 分左右欄）也當表格攤成 `▐▛███▛█ | What's new`，還繞過雜訊過濾＝手機一堆垃圾。修法＝`TidyForPhone` 改**有狀態的表格偵測**：只有偵測到 box 邊框（頂框 `┌──┬──┐`→`inTable`、底框 `└──┴──┘`、分隔列 `├──┼──┤`）區間內的 `│` 列才攤平（真資料表 claude 一律畫成格線）；**不在表格內、中間夾 `│` 的行＝歡迎/changelog 裝飾框 → 整行丟棄**（選擇題框 `│…│` 走前面的 `QuestionLineRe` 保留、一般答覆用 ASCII `|` 不受影響）。另補雜訊規則：spinner 重音動詞（`✻ Flambéi`）、`← 3 agents`、`connecting…`。移除舊 `FlattenTableLine`/`BoxFrameRe`/`TextCharRe`/`MdSepCellRe`。驗證：重建「打 hi」情境（歡迎框+spinner+`❯ hi`+真表格）→ 只留 `● Hi! …` 與攤平表格；選單（bare `❯ 1.` 與 `│…│` 框）完整保留；1.0.36=**遠端上線通知**：`PollLoop` 連上後送「🟢 AwayTerminal 已開啟，遠端上線…」，與 1.0.35 關閉時的「🔴 離線」成對（1.0.34 曾應使用者要求移除開場訊息，加了離線通知後使用者要求配回上線通知）；1.0.35=**遠端關閉離線通知＋推播雜訊修（實測回報）**：① **關閉時發離線通知**（使用者選此輕量方案，不加常駐程式）：`TelegramRemote.NotifyOfflineBlocking`（獨立 HttpClient、阻塞 ≤2s）在 `OnClosingAsk` 的 `Stop()` 前送「🔴 AwayTerminal 已關閉，遠端離線中…」，讓手機知道之後指令不會有回應（正常關閉才送；強殺/當機收不到屬預期——程式沒開就沒有東西 poll bot，這是架構限制）；② **`❯ 你的提問` 回顯過濾**：完成推播會把使用者輸入在 claude 輸入列的回顯當「新內容」，第二次忙→閒又推一次＝重複空訊息（實測「這是測試」收到兩則）。`NoiseLineRes` 加 `^❯ (?![0-9]+[.．])`＝濾掉提示回顯、**但保留選單 `❯ 1. Yes`**（負向前瞻）；③ **`/rc` 碎片**：狀態列右側斜線提示單獨成行漏出 → 加 `^/[A-Za-z]{1,5}$`；④ 送指令後即時回推改 `auto:true`＝沒新輸出就不送（AI 還在想時不回空回音，答案由完成推播帶出）；1.0.34=**遠端再兩項（使用者要求）**：① 移除「AwayTerminal 遠端已連線 ✅」開場訊息（`PollLoop`，預設安靜不主動打擾；那則與「🟢 xxx 閒置（完成）」都被點名為多餘——後者是 `_notify` 分支、1.0.33 起預設關已不出現）；② **手機表格可讀性**：claude 回答常是 box-drawing 表格（`┌┬┐│├┼┤└┴┘`），手機字型 CJK 對不齊＝整團亂，整段刪又會把「答案就是表格」清空（實測「只要表格」的提問刪光後手機端全空＝不好）。改在 `TidyForPhone` 前置 **`FlattenTableLine` 把表格攤平成每列一行「儲存格 | 儲存格 | 儲存格」**：資訊全留、手機好讀（實測 claude 星期表 → `星期一 | Monday | Mon` 逐列，框線/分隔列/狀態列都乾淨）。判定：含 box `│`＝claude 表格直接攤平；只含 ASCII `|` 需「開頭結尾都是 |」才算 markdown 表格（避免把狀態列 `… | [main] | …` 誤判）；純框線（`┌┬┐├┼┤` 無文字）與 markdown 分隔列 `|---|` 丟棄；單欄（選單/框內單行）交回既有處理。另備 **`/plain on|off`**（預設關）＝手機提問（≥8 字、非純數字）附加「請用純文字、不要表格」讓 AI 直接純文字回答（表格本來就會攤平，這是想從源頭避免時的選項）。驗證工具 scratchpad `ptyprobe`（真跑 claude 出表格）+ `tidytest`（反射叫私有 `TidyForPhone`）；1.0.33=**遠端三項調整（使用者要求）**：① `/goto` 不帶編號＝直接顯示分頁清單＋按鈕點選（`DoGoto` 改呼叫 `SendTabList`），`/list`／`/status` 退為隱藏別名、從 `HelpText`／`setMyCommands` 移除，選單/訊息中的 `/list` 字樣改 `/goto`；② **預設不主動推播**：`RemoteNotify` 預設 `true→false`（「其他未進入分頁完成也通知」），附著分頁的 follow 推播不受影響＝「/goto 進入分頁後才會收到訊息」；遠端設定的推播勾選與 `remote.notify` 文案改「其他（未進入的）分頁完成也推播通知」；③ **閒置 10 分鐘靜默離開**：`CheckIdleAsync` 10 分鐘那則「已自動離開」訊息移除（9 分鐘警告保留）；1.0.32=**累積型注音「文字亂位」定位並修正**：使用者回報「單字即時上字正常，但累積組一整串注音（一次組『一二三』再提交）就亂位」。定位＝**逐字送的副作用**：claude 每收一字重繪整條輸入列，v1.0.28 起的「逐字、每 25ms」送法把 claude 自身的輸入列回顯 off-by-one 殘影拉長成多個可見畫格；單字只重繪一次故看不到。修法＝`terminal.js` `sendTyped` **改回整段一次送**（保留 ESC[I 犧牲事件；= doPaste 路徑），claude 只重繪一次。移除 1.0.30 的 `QUIET_MS`/`QUIET_MAX_MS` 自適應節流與 `rec.lastOut`。ptyprobe + headless xterm 五種送法重播全部渲染 `❯ 一二三` 正確、證明非寬度/渲染 bug（見「主要功能行為→claude 分頁輸入佇列」）；1.0.31=**ConPTY 行程結束偵測（真兇修正）**：`ConPtySession` 原本只在**讀取管線 EOF** 時發 `Exited`，但 **ConPTY 的 conhost 在子行程結束後不會關輸出管線**（要等我們 `ClosePseudoConsole`），所以 claude 按 Ctrl+C 離開／powershell 打 exit／adb 斷線後 `Exited` **永遠不觸發** → 1.0.30 才加的「連線已結束→要關閉分頁嗎」對話框根本不會跳（使用者實測 Ctrl+C 後沒反應）。改用 **`ThreadPool.RegisterWaitForSingleObject` 等 `hProcess`**（不佔執行緒；觸發後 sleep 150ms 讓 conhost 把最後輸出送完再發 `Exited`，`Interlocked` 保證只發一次；`Dispose` 先 `Unregister` 再 `TerminateProcess`，使用者主動關不會被當自然結束）。scratchpad `ptyprobe` 端到端驗證：修正前 Exited=NO、修正後子行程結束 160ms 內 Exited=YES。**另查證「中文/ASCII 輸入列 `❯h i` 亂位」**：用 ptyprobe 抓 claude 2.1.237 真實位元組流 + headless xterm 重播，Unicode 6/11 兩種寬度表都渲染成正確的 `❯ hi`，且 DSR 探針確認 conhost 與 xterm 對 ❯/box-drawing/bullet 的寬度判定一致（皆 1、中文 2）＝**不是我方寬度或渲染 bug，是 claude 自身輸入列回顯的暫時 off-by-one，Enter 重繪即正**（與既有踩雷「第一字後空一格」同源）；穩妥繞法＝分頁列「…」輸入框；1.0.30=**使用者六項回饋**：① Claude/Custom/Adb 分頁 session 結束跳「要關閉這個分頁嗎」（`AskCloseExitedTab`）；② 自訂連線與 ADB 分頁**也做開機恢復**（`SavedTab.Name`/`Dir`/`Path` 補齊、`OpenCustom(restoreTitle)`、`RestoreTabs` 加 custom/adb case）——之前只有 ps/ssh/telnet/com 會回來；③④ claude 逐字節流改**自適應**（等輸出靜止 30ms、上限 150ms；`QUIET_MS`/`QUIET_MAX_MS`）；⑤ 分頁列最左新增「**…**」鈕開 `ComposeDialog` 先打好再整段送出（繞過 claude 逐鍵 IME 問題的穩妥路徑）；⑥ 工作列 overlay 改**閒置無球、忙碌紅球上下跳動**（`SetTaskbarBusy`/`MakeBounceFrame`）；1.0.29=**「關於」改版＋著作權補本名**：作者 email 圖片改回 `awaysu@gmail.com`（1.0.4~1.0.28 誤植 weisu.tech@gmail.com），版式改為與 AwayPhotoRawEditor 一致的七段（版本 / 編譯時間 / 作者 / 下載 / Source Code / 授權 / 第三方元件），並在 LICENSE、csproj（`Copyright`＋`Company`）、installer.iss（`AppPublisher`／`AppCopyright`／`VersionInfoCopyright`）、README、THIRD-PARTY-NOTICES、「關於」授權那行**全面標上著作權人本名 Chih-Wei Su (Awaysu)**——Certum 開源憑證要能從公開紀錄把證件本名連到專案；1.0.28=**注音輸入修正三合一**：① claude 分頁新增**輸入佇列＋逐字節流**（`terminal.js` `sendTyped`/`qPush`，見「主要功能行為→Claude Code」）——IME 片語提交不再以「多字元塊」直達 claude，修掉「按過 Esc 後整句被吞」「建議顯示中重複/亂碼」一族問題（根因與實驗見踩雷「claude 多字元輸入塊」）；② `App.OnStartup` **歸零 std handle**——從有重導的環境（腳本/工具）啟動時，pipe handle 值會被 CreateProcess 原樣帶給 console 子行程、ConPTY 不覆蓋非空值，powershell/claude 一寫 stdout 就死＝**待辦 0「PowerShell 分頁一開就死」真兇，已解**；③ 清環境變數改**字首掃描 CLAUDE\*/ANTHROPIC\***（列舉清單追不上 claude 新增變數：2026-08 已出現 CLAUDE_CODE_EXECPATH、CLAUDE_EFFORT）；1.0.27=「清除畫面」改為**一律先跳確認**（見「主要功能行為」）＋工具列該鈕右邊新增「**翻頁**」下拉鈕（`BtnPage`／`Page_Click`，做法同 New：建 `ContextMenu`、依功能列位置決定 Bottom/Right placement）：上一頁 / 下一頁 / 分隔線 / 移到最上面 / 移到最下面，走新的 `S` 協定交給 xterm 的 `scrollPages(∓1)`／`scrollToTop()`／`scrollToBottom()`，**只動檢視、不送任何輸入**（與「清除畫面」對 PowerShell/SSH 送 Esc+Ctrl+L 的做法不同），捲完 `Web.Focus()`＋`term.focus()` 把焦點還給終端機。圖示 `icon/page-scroll.png`＝疊頁+上下箭頭，由 System.Drawing 產生以吻合家族風格（128×128、透明底、描邊 `#303030`、填色 `#CDCDCD`；深色工具列上只看得到淺色填充，故不與相鄰的「清除畫面」外框、「複製全部」疊塊混淆）；1.0.26=常用字串新增「送出後送 Enter」勾選（`PromptItem.SendEnter`，編輯區內容上方；新項目預設不勾、種子的 ls/cat 預設勾）：勾選者送出後 200ms 補對 session 寫 CR 直接執行（`SendSnippet`；Enter 不能併進貼上內容——claude 分頁換行會轉 ESC+CR 軟換行、bracketed paste 下換行也只是插入，都不會送出）。PromptDialog 送出按鈕以按下當下勾選為準、快速輸入抽屜按所選項目旗標；備份/載入 XML 與所有複製點都帶 SendEnter；1.0.25=**「目錄選擇沒蹦出來」真兇定案（diag.log 實錄）**：啟動後頭幾秒 WebView2 尚未 ready，`OpenCustom` 等六個進入點的 `if (!_webReady) return` 把點擊**靜默吞掉**——使用者「開程式馬上點 New→ClaudeCode」必中，重開程式特別容易重現；1.0.22 的 ContextIdle 假說撤銷（1.0.24 已移除該行，但它只是共犯不是主因）。修法＝`DeferUntilWebReady`：ready 前的動作排進 `_pendingReadyAction`（一格、後蓋前），`ready` 處理完恢復分頁後補執行；六個進入點（OpenCustom/ReopenHistory/OpenPowerShell/OpenSsh/OpenCom/OpenAdbFlow）全改；1.0.24=移除 1.0.22 在 PickWorkDir 加的 `Dispatcher.Invoke(空, ContextIdle)`——它沒有完成時間上限，使用者見沒反應而晃滑鼠/連點時佇列不閒、Invoke 永不返回＝對話框永遠不開（2026-08-11 實際發生於 1.0.22，UI 其餘正常故事後採證不到；自動化測試點完就靜止所以 30/30 全過抓不到）。新增 `Services/Diag.cs` 輕量診斷 log（`%LOCALAPPDATA%\AwayTerminal\diag.log`，256KB 砍半），OpenCustom/PickWorkDir 各步驟留時間戳，再發生可直接定位；1.0.23=常用字串「沒改也蹦尚未存檔」修正：群組 editable ComboBox 的內部文字框在視窗第一次顯示、模板套用時才建立，會補發一次值沒變的 TextChanged，把建構式載入的第一筆（有群組者）誤標成髒。`MarkDirty` 改為與 `LoadEditor` 快照實際比對（`SnapshotEditor`），旗標式判髒廢除；順帶「改了又改回原樣」不再算髒。1.0.22=三修：PickWorkDir 開對話框前先 flush Dispatcher＋`Activate()`、LastDir 的 `Directory.Exists` 丟背景執行緒最多等 300ms（休眠硬碟/斷線網路磁碟會卡 UI 數秒＝「目錄選擇沒蹦出來」的主因，逾時就不預選）；遠端設定未勾「啟用」時 Token/ChatId/取得chat id/推播全部鎖灰（TextBlock/CheckBox 用 Opacity 呈現，深色前景寫死蓋不掉停用觸發器）；組字預覽改「依內容判斷」——含英數字=鍵值殘影才隱藏、純注音立刻顯示（舊法每次變動先藏 30ms＝打字閃爍消失；含字母永久隱藏＝英數組字整段看不見），加 250ms 後備顯示；遠端自動推播移出 UI 執行緒（原本 `GetRecentText` 的 `Wait(1500)` 卡 UI 且必逾時退回位元組流備援）、ExitDialog 等待更新 CLAUDE.md 期間按 X 不再 crash、「更新 CLAUDE.md」改用 `TerminalTab.ClaudePaste` 判斷（自訂 ClaudeCode 沒 Restore、舊判斷永遠 false）、Telnet 改非同步連線（主機不通不再凍 UI ~20s，連不上=紅字+Exited 交給重連）、session 結束一律 Dispose（重連不再累積 handle/timer，`OnSessionExited` 改帶 session 實例防過期事件）、COM 拔線會發 Exited（自動重連才會動）、log/遠端緩衝/巨集 wait 改用 stateful UTF-8 Decoder（中文字跨 chunk 不再變 �）；1.0.20/1.0.19=常用字串新預設＋範例只放一次；1.0.18=ADB 改為一般自訂連線、預設自訂清單為空；1.0.16=WinForms 對話框補 owner；1.0.13=不再打包 adb、安裝檔改框架相依、MIT 授權、遠端推播降噪；1.0.12=安裝檔不再匯入根憑證；1.0.11=右鍵「複製且貼上」；1.0.10=claude 分頁貼上改 ESC+CR）（1.0.0=2026-07-27 里程碑、repo 重建＋安裝檔發佈；1.0.4~1.0.28「關於」email=weisu.tech@gmail.com、1.0.29 起改回 awaysu@gmail.com，一路都是圖片渲染；1.0.5 起最下方顯示「編譯時間: yyyy/M/d HH:mm:ss」＝exe 檔案寫入時間，複製/安裝會保留時間戳）。「關於」是自訂深色小視窗（非 MessageBox）、內容置左，1.0.29 起七段：標題／版本／編譯時間／作者（`作者: ` 與名字同一行，名字 `Awaysu (awaysu@gmail.com)` **以執行期渲染的圖片顯示**——`RenderTextImage`，依 DPI 畫、防文字收集）／下載（awaysu.cc）／Source Code（GitHub）／授權（`MIT　© 2026 Chih-Wei Su (Awaysu)`，與 LICENSE、csproj `Copyright` 同一串）／第三方元件。下載與 Source Code 兩行走同一個 `MakeLink` 區域函式。
- **i18n**：使用者可見字串走 `Loc.T`；工具列文字：新連接(New) / 紀錄 / 輸入文字(Compose，1.0.46) / 複製 / 純文字貼上 / 複製全部 / 清除畫面 / 翻頁(Scroll) / 視窗分割 / 常用字串 / 遠端設定 / 設定 / 關於（「功能列」切換鈕 1.0.46 移除）。
- **設定持久化**：記住值都進 `AppSettings.Current` + `.Save()`。
- **範例資料只放一次（v1.0.19）**：常用字串與資料夾書籤的預設範例改由 `SeededSamples` 旗標控制，**只在第一次建立設定檔時放入**。舊寫法 `if (Count == 0)` 等於「清單一空就補回來」，使用者全刪掉、下次啟動又長回來（與自訂連線同一類困擾）。自訂連線則完全不給預設（見「主要功能行為」）。
  - **測試「全新安裝」的方法**：`Environment.GetFolderPath(LocalApplicationData)` 走 shell folder API、**不讀 `LOCALAPPDATA` 環境變數**，所以無法用改環境變數來隔離。實測要「備份 → 把 `settings.json` 移開 → 啟動 → 檢查 → 移回」。
- **分頁命名**：`NextName(prefix)` → 「型態(數字)」，例 PowerShell(1)、ADB(1)、自訂用「名稱(數字)」；跳過已存在名稱。
- **狀態圓點**（分頁左側）：綠=可輸入、**紅=忙（`#F44336`，1.0.42 由橘 `#FF9800` 改紅，與工作列彈跳球同色）**，純活動偵測（已移除 BEL 那套）。**工作列 icon 右下**另有 overlay（`TaskbarItemInfo.Overlay`）：**1.0.30 起閒置／無分頁＝不顯示，有分頁忙碌＝紅球上下跳動**（`SetTaskbarBusy`：預先畫 12 格 16×16 彈跳影格 `MakeBounceFrame`（sin 曲線、貼地壓扁），忙碌時 60ms 計時器輪播、閒置停計時器並清 overlay；舊版是綠=閒/橘=忙兩顆靜態點，使用者嫌閒置也有球太吵）。
- **強調色**：`#FDFFB0` 淡黃（作用中分頁項目框、終端機外框、分割 pane 框、右上 ▼/▲）。1.1.0 起分頁在右側列表框、作用中＝黃框＋#1E1E1E 底，不再與終端機外框融合（舊的 -1px 蓋線已移除）。

## 主要功能行為
- **New（新連接）下拉**（工具列最左）：PowerShell → SSH/Telnet → 連接埠 → **分隔線** → 你的自訂連線（ClaudeCode、ADB、opencode…）→ 分隔線 → 「自訂…」(開管理視窗)。項目為圖示+文字。
  - **分組原則**：第一區＝**AwayTerminal 自己實作**的連線（ConPTY／Telnet／序列埠，不依賴外部程式）；第二區＝**使用者自己管理的自訂連線**，全新安裝為空。
  - **ADB 已完全去特殊化（v1.0.18）**：不再是內建選單項目、設定視窗也移除了 ADB 區塊，改用「自訂… → 自動偵測」一鍵加入（`CustomConnDialog.KnownTools` 已含 `ADB`／`adb.exe`／args `shell`）。**刪光自訂連線後第二區就是空的**——1.0.17 以前 ADB 刪不掉，使用者實際回報過這個困惑。
  - **但裝置選單沒有流失**：`OpenCustom` 以 `IsAdbExe()` 判斷執行檔名是 adb（同 `IsClaudeExe` 的作法），是的話轉呼叫 `OpenAdbFlow()` 走 `adb devices` → 0 台提示／1 台直接開／2 台以上選序號。若直接當成一般自訂連線跑 `adb shell`，接多台時只會噴錯。
  - **`EnsureDefaults` 不再自動建立任何自訂連線（v1.0.18）**：舊的「ClaudeCode 自動遷移」已移除——它會讓使用者「明明全部刪掉了」的清單又冒出東西。`ClaudeMigratedToCustom` 旗標保留於 JSON 相容舊檔但不再使用。
- **Claude Code**：不再是內建按鈕，已**遷移成一筆自訂連線「ClaudeCode」**（`EnsureDefaults` 只做一次、以 `ClaudeMigratedToCustom` 旗標記住）。**直接以 ConPTY 執行 claude.exe（不經 PowerShell）**：claude 即主行程、一開始就用目前尺寸建立，**已移除舊的「等尺寸才送指令」hack**；勾「使用 PowerShell」或路徑是 .cmd/.bat（npm 版）時才走 PowerShell。
  - **claude 分頁輸入佇列（v1.0.28 建立、v1.0.32 改整段送、v1.0.43 加靜止閘門）**：`terminal.js` 的 `sendTyped`/`qPush`/`isTypedText`。**v1.0.43 靜止閘門**：`qPush` 加 `gate` 旗標；IME 整段、貼上、backspace 這幾類會 gate＝在 claude 近 `QUIET_MS`(設定 `ImeQuietMs`，預設 20ms) 內有輸出（＝正在重繪上一筆）時先退回佇列(unshift 保序)稍等，入列超過 `QUIET_MAX_MS`(150ms) 則一定送。目的是別在 claude 重繪過程插入下一筆造成二倍/backspace 殘影（根因在 claude 端，見「版本」1.0.43 三方交叉驗證）；一般 ASCII/Enter/Ctrl 不 gate、即時。打字產生的「純文字提交」（多字元、或含非 ASCII＝IME 提交）→ 先送一個 **`ESC[I`（focus-in，claude 有開 DECSET 1004）當犧牲事件**吸收 claude 可能懸置的 ESC/Ctrl+C 待確認狀態，再**把整個片語一次送出（delay 0）**。**演進**：v1.0.28 原本是「逐字、每 25ms」送（模仿 Windows Terminal 逐鍵），v1.0.30 再加「自適應節流」（等輸出靜止再送下一字）——但**逐字有個致命副作用（使用者 2026-08-20 回報並定位）：claude 每收一字就重繪整條輸入列＋建議文字，逐字送＝把 claude 自身「輸入列回顯 off-by-one 殘影」一次拉長成好幾個可見畫格 → 累積型注音（先在注音框組好「一二三」再提交）看起來就是「文字亂位」；單字即時上字只有一次重繪、太快看不到，所以不發生（完全對上回報）**。v1.0.32 改回整段一次送（= doPaste 同款路徑），claude 只重繪一次、殘影太快看不見。**驗證（scratchpad `ptyprobe`，claude 2.1.237 + headless xterm 重播 raw-*.bin，2026-08-20）**：block／blockesc／char25／char120 五種送法最終畫面**都**正確渲染成 `❯ 一二三`（證明亂位不在最終位元組、也不是 xterm 寬度問題——DSR 探針確認 conhost 與 xterm 對 ❯/框線/bullet 寬度判定一致），差別只在「逐字送會在過程中把 claude 的暫時殘影攤開」。**犧牲事件不能用 NUL**——沒有懸置狀態可吸收時 NUL 會被插進輸入框變成一格隱形字元（`❯一[1C]二三` 實測）；ESC[I 沒狀態時是合法終端機事件、no-op。所有輸入（含貼上）走同一佇列保序（緊接的 Enter 不會超車）；單一 ASCII 按鍵/控制鍵 delay 0 即時送；claude 分頁貼上（doPaste）＝犧牲事件＋整段一次送。**diag 實錄（2026-08-15）證明 xterm 端每次 compositionend 只發一次 onData**（textarea 殘值累積成 `一二三一二三一二三` 也只送一次「一二三」）——重複／亂位都在 claude 端，別再往 xterm/IME 層找。IME 診斷開關 `IMEDBG`（terminal.js 頂端）平時 false，追問題改 true 重建看 keydown/composition/onData 全軌跡。仍要 100% 不經逐鍵回顯＝分頁列最左「…」輸入框。
- **自訂連線**（`CustomConnDialog`）：左清單（可收合）+ 底部「自動偵測 / ＋新增 / －刪除」+ 右編輯（圖示下拉 32×32 無字 / 名稱 / 執行檔+瀏覽 / 參數 / 關閉按鍵〔無·Ctrl+C·Ctrl+D〕×〔x1~x5〕/ 隱藏 / 啟動前選擇資料夾 / 使用 PowerShell）+ 「儲存 / 返回」。髒資料追蹤、切換/關閉/刪除前提示。**自動偵測**：在 PATH 與常見位置找 claude/wsl/opencode/gemini/aider 加入。「隱藏」= 不列在 New 下拉。**自訂分頁 1.0.30 起也做開機還原**（`OpenCustom` 設 `tab.Restore`，Type=`custom`、`Dir`=實際工作目錄、`Name`=連線名稱；`RestoreTabs` 的 `custom` case 直接用存的目錄重開、**不跳資料夾框**，目錄不在了且 PickDir 則退回桌面；`restoreTitle` 參數沿用上次分頁標題）。ADB 同樣恢復（Type=`adb`、`Path`=當初的 adb.exe＋序號，直接 `adb shell` 不再先跑 `adb devices`；裝置不在 adb 自己報錯結束 → 交給「連線已結束→要關閉分頁嗎」）。1.0.29 以前只有 ps/ssh/telnet/com 會回來＝使用者回報「恢復分頁只有 PowerShell 有效」的原因。
- **ADB**：**v1.0.13 起不再內建 adb**（授權原因見下），改用 `AppSettings.ResolveAdbPath()` 搜尋使用者既有安裝——順序：設定裡指定的 `AdbPath` → `PATH` → `ANDROID_HOME`/`ANDROID_SDK_ROOT` → Android Studio 預設位置 → **舊版殘留的 `tools\adb\adb.exe`**（1.0.12 以前裝過的機器照樣能用，升級不會突然壞掉）。找不到 → `PromptInstallAdb()` 說明並詢問是否開啟官方下載頁。**設定視窗有「ADB / adb 路徑」欄位**（v1.0.14 補；`SettingsDialog` 第三個 GroupBox，含瀏覽鈕），**留空＝自動偵測**，提示行會即時顯示目前實際會用到哪一支 adb（或找不到），讓「留空」不是黑箱。找到後行為不變：先 `adb devices`，0 台提示、1 台直接開、2 台以上跳選單，再以 ConPTY 跑 `adb shell`。
  - **為何移除**：Android SDK 條款 §3.4 禁止轉散布 SDK，與 §3.5「另有授權的開源元件」的界線不明確；上架 Store 需對散布內容擁有明確權利。且會用 ADB 的人幾乎都已安裝 platform-tools，為少數人背 8.5MB 與法律模糊地帶不划算。**另有一個確定的缺失**：原本只複製了 4 個二進位檔，沒有帶 Google platform-tools 內的 `NOTICE.txt`，那是 Apache-2.0 §4(d) 明文要求的——不散布之後這個義務一併消失。
  - **踩雷**：`CopyToOutputDirectory` **不會刪除已從專案移除的檔案**，`bin\publish` 會留著上一次的 `tools\adb\`。移除打包內容後必須**先刪 `bin\publish` 再重跑 publish**，否則安裝檔照樣把 adb 包進去（2026-08-04 實際踩到）。
- **紀錄按鈕**（New 右邊，icon `history.png`）：下拉列最近 10 次連線（圖示+標籤），點選以相同設定重開。每次開連線都記錄（最新在前、去重、上限 20），存於 `AppSettings.History`。開啟邏輯抽成 `OpenPowerShellDirect`/`OpenTelnetDirect`/`OpenSshLoginAs`/`OpenComDirect`/`OpenAdbShell`/`OpenCustom` 共用。
- **常用字串**（`PromptDialog`，原「Prompt」）：風格同自訂連線。**群組（方式 B）**：`PromptItem.Group`；左清單依群組顯示**可收合標題**，群組標題**右鍵可改名/刪群組**（刪群組問：連字串刪 / 移到未分組 / 取消）。編輯區有「群組」可編輯下拉。**備份/載入 XML**（左下兩鈕；載入取代目前清單）。
- ~~快速輸入抽屜~~ **1.0.46 移除**（原工具列右端 ▼ 開右側 1/5 寬面板）：併入工具列「輸入文字」視窗上方的常用字串列（見下一條）。`QuickPanel`／`QuickToggle`／`Quick*` 六個方法／`_quickItems` 全部刪除。
- **SSH**：無帳號欄。連線後顯示 `login as:`（PuTTY 式，`HandleLoginInput`/`OpenSshLoginAs`），輸入帳號才啟動 `ssh.exe user@host`；主機欄可直接打 `user@host`。**1.1.1 起 `SshCommand()` 帶 `-o SendEnv=CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN`**：遠端跑 claude 才不會走 alternate screen 把對話從 scrollback 洗掉（症狀＝「claude 回覆後之前的對話不見了」）。只在遠端 sshd `AcceptEnv` 允許時生效，否則靜默忽略——不收的機器請在遠端 rc 檔自行 `export`。
- **關分頁**：背景執行緒送優雅結束鍵（`GracefulExitBytes`，預設 Ctrl+C ×3；SSH=Ctrl+D ×3；自訂可設 無/Ctrl+C/Ctrl+D ×1~5）→ 60ms 後 TerminateProcess → ClosePseudoConsole。不卡 UI。
- **session 自己結束**（`OnSessionExited`）：印灰字 `[連線已結束]`；SSH/Telnet/COM 勾自動重連 → `ScheduleReconnect`；**Claude/Custom/Adb 分頁 1.0.30 起跳 Yes/No「連線已結束，要關閉這個分頁嗎？」**（`AskCloseExitedTab`，`msg.exitedCloseAsk`；Yes→`RemoveTabSilently`）。程式關閉流程中（`_exiting`）不問；PowerShell/未勾重連的 SSH 等維持只印灰字（使用者只要求 adb／claude）。
- **工具列「輸入文字」鈕（1.0.30 為分頁列最左「…」，1.0.46 移到「複製」左邊並併入常用字串）**：`BtnCompose`（圖示 `icon/compose.png`＝文字框＋I-beam，System.Drawing 依家族風格產生）→ `Compose_Click` → `ComposeDialog(sendEnter, Prompts, sendNow)`（深色、可調大小、多行 TextBox＋「送出後送 Enter」勾選（記憶於 `ComposeSendEnter`）＋「清除／復原／返回／送出」，Ctrl+Enter=送出）。**上方常用字串列（1.0.46）**：群組（(全部)／各群組／未分組）與標題下拉、「**插入**」把所選字串放進文字框游標處（走 `SelectedText`＝進 undo 堆疊）、「**直接送出**」不經文字框、依該字串自己的 `SendEnter` 立刻走 `sendNow`（＝`MainWindow.SendSnippet`），**視窗留著可連送**，按鈕短暫變「✓ 已送出」；下拉列下方**唯讀 TextBox `PreviewBox`** 顯示所選字串完整內容（可選取複製），與輸入框 `Input` 以 Grid `2*`:`3*` 分高度（使用者指定輸入框＝原本的 3/5）；上次選的群組／標題記在 static（再開直接按「直接送出」重送）。沒有分頁時按鈕旁浮出 `compose.noTab`。**草稿保留**：打到一半按 X 或「返回」，下次叫回來文字還在（static `_draft`，程式存活期間；「送出」才清空）。「清除」走全選＋取代選取（進 undo 堆疊）並另存 `_lastCleared`；「復原」先 `TextBox.Undo()`（逐步、含清除），堆疊空且框空時放回 `_lastCleared`（關掉重開後 undo 堆疊是新的，靠這個救）。**用途＝先在一般 WPF TextBox 把中文打好再整段送**：IME 組字/提交都不經 xterm→ConPTY→claude 的逐鍵解析，繞過 claude 端的重複／亂碼；送出走 `SendSnippet`（claude 分頁多行→ESC+CR、勾 Enter 則 200ms 後補 CR）。沒有分頁時按下顯示浮動提示 `compose.noTab`（不無聲返回）。
- **關閉程式**（`ExitDialog`）：兩選項「下次開啟恢復目前分頁」「Claude Code 離開前更新 CLAUDE.md」（**勾選狀態記憶**於 `ExitRestoreTabs`/`ExitUpdateMd`）。確認後存 SavedTab（ps/claude/ssh/telnet/com，**1.0.30 起含 custom/adb**）→ 快速終止 → **`Environment.Exit(0)`**（跳過 WebView2 冗長 teardown）。**1.0.45 起改 `FinishExitAsync`（非同步）**：勾恢復時先 `q…save` 取各分頁 scrollback（含顏色，≤2.5s）存 `%LOCALAPPDATA%\AwayTerminal\restore\tabN.txt`，`SavedTab.BufferFile` 記檔名；勾選文字改「下次開啟恢復目前分頁（含畫面上的舊訊息）」。
- **啟動**：`ready` 後只恢復上次存下的分頁；**沒有就保持空白（不再硬開預設 PowerShell）**。**1.0.45**：有 `BufferFile` 的分頁先把舊訊息倒回 xterm（灰色分隔行標上次關閉時間）再啟動連線；命令列 `--open-dir` 的資料夾在恢復之後開啟（成為作用中分頁）。
- **檔案總管右鍵「用 AwayTerminal 開啟」（1.0.45）**：資料夾上或資料夾內空白處右鍵 → 在該目錄開 PowerShell 分頁（分頁名＝資料夾名）；已有 AwayTerminal 在跑就直接加分頁並拉到前景（具名管線轉交，第二個行程立刻結束）。設定→「檔案總管」可關（移除 HKCU 登錄）。細節見「版本」1.0.45 ⑤。
- **分頁 tooltip**：`標題 時:分`（已啟動時間，例 `PowerShell(1) 00:23`），狀態輪詢每 0.6s 更新。
- **視窗檢視三態**（`_viewMode`，「分割」鈕循環）：tab → split(grid) → columns(單列橫排) → tab。分割/分欄可拖曳重排、點標題單頁 zoom。
- ~~功能列位置~~ **1.0.46 移除**：`BtnTaskbar`／`TaskbarToggle_Click`／`ApplyToolbarPosition`／`ToolBtnEx.cs`（附加屬性＋template trigger）／`AppSettings.ToolbarPosition`／`icon/taskbar-change.png` 全部刪除；New／紀錄／翻頁三個下拉選單 placement 固定 Bottom。舊 settings.json 的 `ToolbarPosition` 由 `ExtraFields` 原樣保留、無影響。
- **字型/顏色**：`設定`(SettingsDialog) 用 GroupBox 分「語言 / 字體背景顏色」；**Ctrl+滾輪**縮放字級（全域、記憶、`z` 協定）；**分頁右鍵「配色」**逐分頁套色（5 組預設 + 回設定預設，`P` 協定）。
- **記錄 log**：視窗按鈕「開始記錄」；時間戳 `[yy-MM-dd HH:mm:ss]`。
- **遠端控制（Telegram，v0.9.74~76 新增）**：一台 PC 一個 bot（@BotFather 申請 token），`RemoteDialog` 設定 Bot Token / 允許的 Chat ID（一鍵抓）/ 推播開關（`AppSettings.RemoteEnabled/TelegramBotToken/TelegramChatId/RemoteNotify`）。**多開防護（v1.0.1，雙實例實測過）**：具名 Mutex `Local\AwayTerminal.TelegramRemote` 保證同機只有第一個實例啟動遠端（同 token 兩個 long polling 會 409+搶訊息）；其餘實例跳過並在遠端設定顯示橘字提示，關掉持有者後回來按「儲存」即可接手（AbandonedMutex 也視為取得）。**一個 token 只能一台裝置 poll**：第二台電腦要另申請 bot（chat id 可同一個）。`TelegramRemote` 自寫 HttpClient long polling（無第三方套件），啟動時 PrimeOffset 跳過舊訊息、只認設定的 chat_id。指令（**1.0.33 起 `/goto` 不帶編號＝列出分頁點選、`/list` 併入 `/goto`**）：`/goto [n]`（不帶編號＝分頁清單＋按鈕；`/list`/`/status` 仍是隱藏別名）`/new`（**每種連線只列一個**：PowerShell(桌面)/SSH/Telnet/COM/ADB/未隱藏自訂各一，**不列 History**；回數字開啟+自動附著；PowerShell 與 PickDir 自訂一律以桌面為工作目錄）`/ssh [user@]主機[:埠]`／`/telnet [主機[:埠]]`（不帶參數用上次主機；ssh 帶 user@ 直接連、否則走 login as: 回覆帳號）`/history`（只顯示最近 10 筆；`/history <n>` 才用該筆開新連線——**刻意不吃純數字回覆**，數字留給終端機輸入/選單應答；PickDir 者以桌面開）`/shot`（畫面截圖）純文字=打字+Enter `/key <ctrl-c|ctrl-d|esc|tab|enter|方向>` `/stop` `/last [n]` `/more`（上一則輸出往前翻頁，約 3000 字/頁；緩衝=瘦身後的最近 400 行）`/close [n]`（真正關閉分頁，走優雅結束、不跳 PC 確認框；無參數=關附著的）`/where` `/follow` `/notify`（**1.0.33 預設關**＝只有已 /goto 進入的分頁才推播；其他分頁完成要通知才開）`/exit`（只離開檢視不關分頁；**附著後閒置 10 分鐘自動離開，1.0.33 起靜默不推訊息**，9 分鐘先警告，計時只算手機來訊）`/help`；啟動時自動 `setMyCommands` 註冊指令選單（手機有 Menu 鈕）。**follow/完成推播＝只推新輸出（v1.0.3，e2e 驗證過）**：`_baseline` 記每分頁上次推播（或 /goto 附著）當下的原始渲染文字，`DiffNew` 用「基準尾端連續 3 行錨點」（前幾行完整比對＋最後一行前綴比對，prompt 打字後原行變長仍能對上；錨點必須連續取、含中間空行）找出新增行；比對不到（清屏/TUI 大改）自動退回「最後 n 行快照」，`/last` 指令固定快照語意。同分頁連打兩個指令，第二次推播不再包含第一次輸出。其他分頁推一行閒置通知（notify 開時；**1.0.33 起 notify 預設關**）。輸出來源=**xterm 渲染後文字**（`q…text` 協定），再經 `TidyForPhone` 過濾 TUI 雜訊行、取行數、3500 字截斷、`<pre>` 送出。**選擇題**：claude 跳選單時（轉閒推播會帶出題目+選項，`│` 框內的問句/選項行剝框保留），手機**直接回數字** → 遠端偵測畫面有「❯ N.」選單就自動換算成 ↑/↓ 導航+Enter（claude 選單不吃數字鍵，2026-07-27 實測傳 3 正確選中第 3 項）。**inline 按鈕（v0.9.82）**：`/list`（點按=goto）、`/new`、`/history` 清單附按鈕（每列 2 顆），選擇題推播偵測到選單自動附選項按鈕（callback `opt:{tabId}:{n}`、跨分頁拒答），`/close` 一律先出「✅ 確定關閉／✖ 取消」按鈕；long polling 認 `callback_query`（同樣驗 chat id）→ `answerCallbackQuery` 停轉圈，打字流程全數保留。
- **推播噪音修正（v1.0.13）**：使用者回報「在輸入框打字也會推播到手機」。根因＝**打字的按鍵回顯會一直更新 `LastOutputUtc`**，所以打一句話（很容易超過原本的 3 秒忙碌門檻）整段都被判定為忙，停手後 0.5~1.5s 翻閒 → 觸發「完成」推播。修法＝`TerminalTab.LastInputUtc`（只在 JS 的 `i` 協定更新，即真正的鍵盤／貼上），轉閒時若距最後一次按鍵 < 2.5s 就視為打字回顯、不推播。**遠端自己送的指令走 `SendInputToTab`、不經 `i` 協定**，所以從手機下指令仍會正常回推結果。**v1.0.38 修**：上面「只看 2.5s」在**「改在 App 直接打字送出、AI 很快回答」**時會誤判——App 打字更新 `LastInputUtc`，答案又在 2.5s 內回 → 被當打字回顯、不推（使用者實測「一開始手機問有推、改在 App 問就沒推」）。改用 `TerminalTab.LastSubmitUtc`（App 的 `i` 協定收到含 `\r`/`\n`＝按 Enter，或遠端 `SendInputToTab(enter=true)` 時更新）判斷「這段忙碌期間有沒有真的送出過」：**有送出＝真工作 → 忙 ≥0.8s 就推**（含 App 打字送出、快答）；沒送出＝純打字 → 維持 2.5s 回顯抑制＋忙 ≥3s 門檻。送出時間允許比忙碌起點早 2 秒（送出→開始輸出有延遲）。第二個噪音來源＝`SendLast` 在增量模式算不出新輸出時仍會送出「只有標題沒有內容」的空訊息 → 新增 `auto` 參數，自動推播（僅 `OnTabIdle`）無新輸出時整則不送；使用者主動要的（/last、送鍵後回傳、開連線推開場）仍會回一句，否則看起來像壞了。
- **保持連線（v0.9.83~84）**：SSH/Telnet 連線視窗「保持連線」**下拉** 0/1/3/5/10/15/30/60 分鐘（`AppSettings.KeepAliveMins`，預設 10、記憶；0=關）。SSH=`ssh.exe -o ServerAliveInterval={分×60} -o ServerAliveCountMax=3`（`SshCommand()` 集中組指令，四個啟動點共用）；Telnet=`TelnetSession.KeepAliveMins` 計時器送 IAC NOP（**直接寫串流，勿走 `Write()`——它會把 0xFF 轉義成資料**）。
- **斷線自動重連（v0.9.82）**：SSH/Telnet/COM 連線視窗有「斷線自動重連」勾選（`AppSettings.AutoReconnect`，**預設不勾**（0.9.85 改）、記憶）。session Exited 且分頁還在（非使用者關閉）→ 依 `Restore` 重建 session（同分頁），退避 3,6,9…最多 30 秒，一收到輸出歸零；使用者打 exit 登出也會重連（要停就關分頁）。SSH 重連=重跑 ssh.exe（密碼會再問）。**1.0.45**：重連前先送 `b` 協定把舊畫面推進 scrollback（ConPTY 首幀 `ESC[2J` 只清到空白列，舊訊息全留）；**沒勾自動重連**的 SSH/Telnet/COM 結束時印「[按 Enter 在此分頁重新連線]」→ 同分頁 `ManualReconnect`（SSH 沒 user@ 回 login as:）。
- **終端機右鍵選單＋Ctrl+F 搜尋（v0.9.82，1.0.2 改版）**：`ContextMenuRequested` 取代 Edge 預設選單 → **貼上/複製/複製且貼上/複製全部/複製全部存至檔案/分隔線/搜尋**（1.0.11 於「複製」下加入「複製且貼上」＝選取文字進剪貼簿後直接貼回原分頁，走 `q…selpaste`；`q…file` 協定=整個 buffer 純文字→`SaveBufferToFile` 存檔對話框；工具列該鈕仍叫「純文字貼上」、右鍵選單叫「貼上」，行為相同；JS 端 `A`(全選) 協定保留未用）。搜尋列是 **HTML 內的浮動列**（避開 airspace），Ctrl+F 由 `attachCustomKeyEventHandler` 攔截；vendor 無 search addon → 自製 buffer 掃描（translateToString 快篩＋命中行逐 cell 對映欄位處理中文寬字），Enter/Shift+Enter 上下一筆、Esc 關閉、命中行置中＋選取標示。

## 數位簽章 / 安裝檔
- **自簽章（本機用）**：`sign.ps1` build 後自動簽 exe（csproj `SignOutput` target；找不到憑證安靜略過）。**2026-08-13 起改用跨程式共用的「Awaysu」憑證 `CN=Awaysu, O=Awaysu, C=TW`**（AwayPhotoRawEditor 等同一張；舊的 AwayTerminal 專用憑證 `CN=AwayTerminal (awaysu)` 已刪除），在 `CurrentUser\My`（含私鑰）。目前指紋 `997D278FE3FD6FFA1F8E43683047530DE7210C66`（2026-08-13 ~ 2036-08-13）。`installer/AwayTerminal.cer` 就是這張的公開版（**檔名沿用舊名，內容是現行憑證**，1.0.43 出安裝檔時核對過指紋相符）。
  - **仍寫著舊主體、用到前要先改的兩支腳本**：`trust-cert.ps1`（`CN=AwayTerminal (awaysu), O=AwayTerminal, C=TW`）與 `msix/build-msix.ps1` 的 `-CertSubject` 預設值。照原樣跑會找不到／另建一張錯的憑證，`sign.ps1` 之後就簽不到同一張。
  - **installer.iss 的 `certutil -delstore "AwayTerminal (awaysu)"`（[Run] 與 [UninstallRun] 各兩行）刻意保留舊名**：那是清掉 1.0.11 以前安裝檔塞進機器層級的**舊**憑證，本來就該指舊主體，不要跟著改成 `Awaysu`。
- **私鑰刻意不做任何備份**（2026-08-03 決定）：**不進 git、不放雲端**。理由：(1) 推上 git 不可逆——歷史永久保存、GitHub 未被引用的物件無法可靠刪除；(2) private repo 防的是路人，防不了真正的威脅——本機 gh token（repo 權限、存在 keyring）本身就能 clone，等於在憑證存放區之外多一份可被同一個威脅取得的副本；(3) 這把金鑰是 `AllowPlaintextExport`（可明文匯出），且 1.0.11 以前的安裝檔曾把它塞進使用者的**機器層級**受信任根，一旦外流，用它簽的惡意程式在每台跑過安裝檔的電腦上都自動受信任；(4) 產業方向相反——CA/B Forum 自 2023-06 起要求簽章私鑰must放在 FIPS 認證硬體。
  - **遺失時的重建程序**（成本近乎零，故不需備份）：① 跑 `trust-cert.ps1`（找不到憑證時會自動 `New-SelfSignedCertificate` 建新的；**先把它的 `$subject` 改成現行的 `CN=Awaysu, O=Awaysu, C=TW`**，否則建出來的主體與 `sign.ps1` 找的不一致）② 重新 build/publish 讓 `sign.ps1` 以新憑證簽章 ③ **重新匯出 `installer/AwayTerminal.cer`**（安裝檔流程裡那一步）④ 更新本檔與下載頁公布的指紋 ⑤ 通知已執行過 `trust-publisher.ps1` 的使用者重跑一次（舊憑證的信任不會自動轉移）。
  - **注意**：舊憑證簽過的已發佈安裝檔不受影響（有 DigiCert 時間戳，簽章仍有效），重建只影響之後的版本。**`trust-cert.ps1` 是開發用**（找不到憑證會**建立新的自簽憑證**，絕不可給使用者跑）；給使用者的是 `installer/trust-publisher.ps1`（只匯入公開 .cer、只寫 `CurrentUser`、支援 `-Remove`、會印指紋供核對）。
- **⚠️ publish 出來的 exe 不會自動簽章**：`SignOutput` target 掛在 `AfterTargets="Build"`，`dotnet publish` 會覆蓋掉已簽的檔案 → 出安裝檔時必須**手動**對 `bin\publish\AwayTerminal.exe` 與編譯完的 `AwayTerminal-Setup-*.exe` 各跑一次 `sign.ps1`。
- **兩條產線，發佈方式不同（v1.0.13 起）**——**目錄不可共用**，否則後跑的會覆蓋前者（`CopyToOutputDirectory` 不刪舊檔，正是 adb 那次踩到的陷阱）：
  | 通路 | 發佈方式 | 輸出目錄 | 大小 |
  |---|---|---|---|
  | Inno 安裝檔 | `--self-contained false`（框架相依） | `bin\publish` | 24 檔 / 3.2 MB |
  | MSIX / Store | `--self-contained true`（自帶） | `bin\publish-msix` | 488 檔 / 171.6 MB |
  - **Store 版必須自帶**：MSIX 無法像 Inno 那樣偵測並安裝 .NET Desktop Runtime，Store 也沒有桌面 .NET 的 framework package。
  - **安裝檔內含 .NET 9 Desktop Runtime 安裝程式**（`installer/windowsdesktop-runtime-win-x64.exe`，58MB，已 gitignore；`DesktopRuntimeMissing` 為真才執行）。**偵測用資料夾掃描不用註冊表**——`HKLM\SOFTWARE\dotnet\Setup\InstalledVersions\x64\sharedfx` 在 Win10 19045 實測**不存在**（即使 runtime 已裝），改掃 `{commonpf64}\dotnet\shared\Microsoft.WindowsDesktop.App\9.*`。
  - **安裝檔反而變大**：61.8MB（自帶版是 55.6MB）——runtime 安裝程式本身已壓縮、Inno 壓不動，而自帶的 172MB 散檔能壓到 55MB。換到的是**安裝後磁碟 184.8MB → 7.4MB**，且 runtime 由微軟經 Windows Update 修補（自帶＝.NET 安全更新變成你的責任）。
  - **`[InstallDelete] Type: filesandordirs; Name: "{app}\*"` 不可移除**：Inno 就地升級**不會刪除舊版裝過、新版已不含的檔案**。沒有這段，從 ≤1.0.12 升級會留下 ~172MB 孤兒 runtime 檔＋`tools\adb`，框架相依省的空間完全沒實現（實測升級後仍是 184.8MB，加了之後降到 7.4MB）。{app} 內無使用者資料（設定在 `%LOCALAPPDATA%`、log 在文件夾），清空安全。
- **安裝檔（Inno Setup）**：`installer/installer.iss`。流程：`dotnet publish -c Release -r win-x64 --self-contained false` → 簽章發佈的 exe → 匯出 `AwayTerminal.cer`（公開）→ ISCC 編譯 → **再簽 setup exe**。安裝時必要時裝 WebView2（內含 bootstrapper）、建捷徑。
  - **v1.0.12 起安裝檔不再碰憑證存放區**（原本 `[Run]` 有兩行 `certutil -addstore -f Root/TrustedPublisher`，`runhidden` + 管理員權限）。移除原因：這個組合等同 **MITRE ATT&CK T1553.004（Install Root Certificate）**，MITRE 頁面舉的惡意程式範例指令幾乎一模一樣，`certutil` 本身又是知名 LOLBin，**極可能是 PC-cillin 誤判的主因**；且它要求每位使用者信任一張私鑰放在開發機上的根憑證（Superfish／eDellRoot 前例）。改為 `AwayTerminal.cer` + `trust-publisher.ps1` 裝進 `{app}`，**使用者自行決定**是否執行。
  - **兩者刻意分屬不同存放區、不會互相干擾**：舊的壞行為寫 `LocalMachine`（全機器、需管理員），新的 opt-in 寫 `CurrentUser`（僅自己、免管理員）。`[Run]` 保留兩行 `-delstore`**只清 LocalMachine**，讓 1.0.11 以前裝過的機器升級時自動清乾淨，不會洗掉使用者自己的選擇。
  - **踩雷**：`Cert:\CurrentUser\Root` 的檢視是「使用者 ∪ 機器」存放區的**聯集**，直接 `Remove-Item` 會刪到機器層級那張並拿到 Access denied（`trust-publisher.ps1 -Remove` 已逐張 try/catch 並分開提示）。
  - ISCC 路徑：`%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`。
  - **限制（2026-08 查證後大幅修正）**：使用者遇到的其實是**三個獨立機制**——瀏覽器下載警告、SmartScreen 執行警告（前兩者只看**信譽**）、UAC「不明發行者」（只看**簽章身分**）。**憑證只解決第三個。**
    - **「買 OV/EV 就免警告」已不成立**：微軟 2024 年移除了 EV 的 SmartScreen 即時信譽特權（[code-signing-options](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)：「EV certificates previously bypassed SmartScreen entirely on first download... That behavior was removed in 2024」），現在 OV 與 EV 同級，**不要買 EV**。SmartScreen **沒有白名單管道**（微軟明文 "no need (or mechanism)"），只能靠下載量累積。
    - **自簽在 SmartScreen 眼中等同未簽**，且信譽**無法跨版本累積**（每個新 hash 從零開始）——四天發十二版的節奏在數學上永遠累積不到。憑證的真正價值是讓信譽掛在同一個發行者身分下累積。
    - **Smart App Control（Win11 22H2+ 全新安裝）直接封鎖**自簽／未簽執行檔，且不限有 MOTW 的檔案，只認受信任根計畫內 CA 簽發的憑證。
    - **選項成本**：個人買不到 OV（需法人），對應產品是 **IV**。台灣可行的最便宜是 Certum 開源憑證雲端版 **€49/年**（免硬體 token）；SSL.com IV $129/年＋eSigner $20/月。**Azure Artifact Signing（$9.99/月）個人限美加，台灣不適用**。SignPath Foundation 對開源免費但憑證掛他們的名字，且要求「可驗證地從原始碼建置」——**原本 `tools/adb/` 的 Google 預編譯檔是疑慮點，v1.0.13 移除後此障礙已消失**（加上 1.0.13 已補 MIT LICENSE，SignPath 的兩項前置條件都滿足了）。**Microsoft Store 是唯一真正零警告**的通路（個人註冊費已取消），但要改包 MSIX。
    - **winget 有效但只限該通路**：社群來源被標記為 Trusted，下載的檔案拿到 `ZoneId=2` 而非 `ZoneId=3`（本機實測），SmartScreen 因此不觸發；但從 GitHub Releases 直載的人不受惠，SAC 與防毒也不受影響。註：本機用 `winget install --manifest` 測試走的是**非信任路徑**，會看到真實使用者不會遇到的警告（別被誤導）。

## MSIX / Microsoft Store（v1.0.12 起）
- **打包**：`msix\build-msix.ps1`（`-Publish` 重跑 publish、`-NoSign` 產生未簽章套件供上架）。產出 `msix\out\AwayTerminal-<四段版本>.msix`，約 76MB（MSIX 壓縮比 Inno 好，安裝檔是 55.6MB 但解開後 180MB）。圖示由 `make-images.ps1` 從 `icon\app-icon.png` 產生 11 種尺寸。
- **必須是全信任桌面應用**：`EntryPoint="Windows.FullTrustApplication"` + `rescap:runFullTrust`。**絕不可做成 UWP/AppContainer**——子行程會繼承容器，ConPTY 開的 powershell/ssh/adb 全部失去檔案系統與網路存取，程式等於報廢。
- **兩大風險已實測排除（2026-08-03，Win10 19045）**：
  1. **ConPTY 在 `C:\Program Files\WindowsApps\...` 長路徑下正常**——側載後子行程樹確實出現 `conhost.exe` + `powershell.exe`（曾擔心 terminal#16860 的 `CreatePseudoConsole` MAX_PATH 崩潰，未重現）。
  2. **內建 `adb.exe` 可執行**（WindowsAppSDK #4651 的 ACCESS_DENIED 未重現）。注意：**外部行程跑 WindowsApps 裡的 adb 會 Access denied**（ACL 只允許套件自己），這是正常的、不是 bug；要驗證得用 `Invoke-CommandInDesktopPackage -PackageFamilyName ... -AppId AwayTerminal` 以套件身分執行。
- **設定不會沿用（重要）**：封裝後 `%LOCALAPPDATA%` 被重導到 `%LOCALAPPDATA%\Packages\<PFN>\LocalCache\Local\AwayTerminal\settings.json`，**沒有 read-through 回退**——實測封裝版讀不到原本的 3 筆自訂連線／16 筆歷史／Telegram token，等於全新設定。要沿用需宣告 `rescap:unvirtualizedResources`（受限能力，上架要向微軟說明理由；Windows Terminal 就是這樣做的）或在程式內做一次性匯入。
- **上架前置（需使用者操作）**：① 到 **`storedeveloper.microsoft.com`** 註冊個人帳號（費用已取消；從 Partner Center 或 VS 進去會走到舊的 19 美元流程）② 保留應用程式名稱 → 取得 `Identity Name` 與 `Publisher=CN=<GUID>`，同步改進 `AppxManifest.xml` ③ 用 `-NoSign` 產生套件（微軟會以你的發行者身分重簽）④ **隱私權政策是硬性要求**（政策 10.5.1，桌面橋接產品必備）⑤ **Telegram 遠端控制是最大的審查風險**（長輪詢外部伺服器＋執行指令＋截圖，形同 RAT），建議預設關閉並在提交表單誠實說明。
- **簽章與 Identity 必須完全一致**：`AppxManifest.xml` 的 `Publisher` 與簽章憑證主體不符時，makeappx 打包會過但安裝會被判 identity 不符而失敗（`build-msix.ps1` 已在簽章前先擋下並印出兩者差異）。

## 踩雷紀錄（重要）
- **ConPTY 新 session 第一幀一定送 `ESC[2J`（2026-08-28 定案，v1.0.45）**：conhost XtermEngine 首次 StartPaint 會 `_ClearScreen()`；xterm 的 ED 2 是原地清可視區、不推進 scrollback → 在同一個 xterm 接新 session（重連／恢復分頁）時「最後一頁」憑空消失。凡是要在既有 xterm 上接新 session，先送 `b` 協定推進 scrollback（`terminal.js applyRestore`）。**別改用 `PSEUDOCONSOLE_INHERIT_CURSOR`**——它會讓 conhost 發 DSR 等 CPR 回覆，時機不對會把 `ESC[r;cR` 漏進子行程輸入。
- **xterm.js 6.0.0 `_inputEvent` 會與 `compositionend` 重複交付同一段 IME 文字**：IME 以浮動視窗（非 inline）組字時提交走 `input(insertText)`；`_inputEvent` 在 keyup 後（`_keyDownSeen=false`）直接送，`_finalizeComposition` 的 setTimeout 再送一次（`_dataAlreadySent` 只有 `_handleAnyTextareaChanges` 會更新）。對策在 `sendTyped` 去重（同字串＋期間無 keydown＋100ms），命中寫 diag.log `ime-dup dropped`。別在 body capture 層 `stopPropagation` 擋 `input` 事件——Win+. 表情符號面板等只走這條路的輸入會一起被擋掉。
- **恢復 scrollback 必須在 pane fit 到最終寬度之後寫**：在預設 80×24 寫入、之後變寬，xterm reflow 接回長行時 `_reflowLargerAdjustViewport` 會 `ybase--` 把 scrollback 拉回可視區，再被 2J 清掉。`terminal.js` 用 `rec.fitted`／`pendingRestore`／`held` 排序；分頁模式的隱藏 pane 量不到尺寸（display:none），改由 `refit` 用作用中 pane 的 `proposeDimensions()` 同步 `resize`。
- **UI 自動化免搶前景的做法（2026-08-28 實測全通，使用者在旁也不受干擾）**：對話框按鈕用 UIA `AutomationId`（＝WPF `x:Name`，例 ExitDialog 的 `OkBtn`／`RestoreCheck`）`InvokePattern`／`TogglePattern`；ContextMenu／分頁列「▲」清單項目用 UIA `Name`（popup 是同行程的頂層視窗，`RootElement.FindAll(Children, ProcessId)` 找得到）；終端機打字用 `PostMessage` `WM_CHAR`（每字）＋`WM_KEYDOWN/WM_CHAR/WM_KEYUP` VK_RETURN 給 `Chrome_RenderWidgetHostHWND`（xterm 沒焦點時**第一個鍵只被拿去取得焦點**，重要的 Enter 要再送一次）；捲動用 `WM_MOUSEWHEEL`（lParam 要螢幕座標，視窗最小化時座標無效、要先還原）；截圖 `PrintWindow(hwnd, hdc, 2)`。Git Bash 陷阱：`taskkill /PID` 的 `/PID` 會被 MSYS 轉成路徑（改 `Stop-Process`）；`cmd /c start "" x.exe` 的空引號被吃掉、開成 cmd 視窗（直接 `(./x.exe > /dev/null 2>&1 &)`）；PowerShell 腳本裡 `$pid` 是唯讀自動變數。
- **std handle「值」會傳播給 console 子行程＝「分頁一開就死」真兇（2026-08-15 定案，v1.0.28 修）**：AwayTerminal 若從**有重導 stdout/stderr 的環境**啟動（Claude Code 工具、腳本、CI），會繼承對方的 pipe std handle；`CreateProcess` 對 console 子行程會把這些 handle **值**原樣帶過去，**ConPTY 連接只在 std handle 為空時才換成 console handle** → powershell/claude 在子行程裡拿到無效 handle，一寫 stdout 就在 0.5 秒內死掉（分頁只剩游標、conhost 還在）；claude 則誤判非 TTY 走 `--print` 直接退出。使用者從桌面正常啟動不受影響——**這就是為什麼待辦 0 只在開發/測試環境重現**。修法＝`App.OnStartup` 先 `SetStdHandle(-10/-11/-12, NULL)`（GUI 程式用不到 std handle，歸零讓每個 ConPTY 子行程拿到正確 console handle）。同理，**自製 ConPTY harness 從工具環境跑時要 `AllocConsole`＋把自己的 std handle `SetStdHandle` 指到 CONIN$/CONOUT$**，否則 claude 一樣掉進 `--print`（imehost 實測）。
- **claude 對「多字元輸入塊」的解析脆弱（2026-08-15 實驗定案；v1.0.28 以逐字節流繞過）**：IME 片語提交從 xterm 出來是一整塊（onData「一二三」）、一次 WriteText 進 ConPTY。實驗矩陣（imehost harness + claude 2.1.233）：**空輸入框＋大塊＝正常**；**前面有 lone ESC（按過 Esc）＋大塊＝整句被吞**（claude 把 ESC+塊當未知跳脫序列整段丟棄，ESC 懸置 800ms 仍如此）；**Ctrl+C 待確認狀態＋塊＝被吞**；**逐字（30ms 間隔）＝只丟第一字**；**ESC[I 犧牲事件＋逐字＝一字不丟**。Windows Terminal 沒這問題是因為它走 win32-input-mode 逐鍵送、claude 從不會收到多字元塊——v1.0.28 的逐字節流就是模仿這個行為。使用者回報的「注音輸入變成兩份（一二三→一二三一二三）、有時亂碼」即此族群（建議文字顯示中=claude 重繪最忙、輸入事件處理交錯最嚴重時）。另注意 **conhost 輸入轉譯本身無罪**：ReadConsoleInputW 實測每個中文字＝乾淨的 keydown+keyup 對（vk=0、char 正確），大塊 13 字也不重複不亂碼。
- **claude 輸入列的「第一字後空一格」是顯示殘影不是資料錯誤**：placeholder（Try "..."）消失後第一個寬字元的回顯 off-by-one，序列化可見 `❯一[1C]二三`；**送出的 prompt 實測是連續的「一二三」**，且下次整行重繪就消失。逐字或慢速打字都會出現（修正前後皆然、上游行為），別誤判成輸入層 bug。
- **UI 自動化的 SendKeys `^v` 不會觸發 WebView2 的 paste 事件**（2026-08-15 實測數次「貼上沒反應」＝此假象）：合成的 Ctrl+V 打得進 keydown、但瀏覽器不出 paste 事件，右鍵貼上/工具列「純文字貼上」（v 協定）才走得到 doPaste。自動化驗證貼上請點工具列鈕，別用 SendKeys ^v。另 IME 中文模式下 SendKeys 英文字母會被拿去組字（打 "hello" 變注音），要打英文先確定 IME 關閉或用剪貼簿。
- **強殺 AwayTerminal 會留殭屍 conhost 鎖住資料夾**（2026-08-11 實查）：`Stop-Process -Force` 殺掉主程式時 ConPTY 的 conhost.exe 不會被帶走（正常關閉會走 ClosePseudoConsole 才會收乾淨），殭屍 conhost 的 CWD＝當初 exe 的啟動目錄 → `bin` 刪不掉（The process cannot access…）。清法：掃各行程 PEB 的 CurrentDirectory 找出 CWD 在目標資料夾內的 conhost 殺掉即可（診斷腳本模式見 scratchpad cwd-scan）。一般使用者正常關閉不受影響；這主要咬開發/測試流程。
- **多行貼上必須走 `xterm.paste()`（v1.0.7 修）**：舊版把剪貼簿文字直接 `Session.WriteText`，每個換行都被當 Enter 送出 → 前面幾行被執行掉、**只剩最後一行留在輸入框**（claude 尤其明顯）。正解＝新增 `v{id}{US}{base64}` 協定交給 JS 呼叫 `term.paste()`，由它負責 `\r\n`→`\r` 正規化，並在程式啟用 bracketed paste（DECSET 2004）時包上 `ESC[200~/201~`；之後照常經 `onData`→`i…` 回送。工具列「純文字貼上」、右鍵「貼上」、常用字串抽屜、PromptDialog 全部走 `PasteToActive()`。註：Windows PowerShell 5.1 內建的 PSReadLine 較舊、未必啟用 bracketed paste，該情況下多行仍會逐行執行（Windows Terminal 亦同）；vim/bash 則正常。
- **claude 分頁多行貼上「有時候分開貼上」（v1.0.10 修）**：實測（ConPTY harness + stdindump/claude 2.1.220）**Win10 19045 conhost 會把輸入流的 `ESC[200~`/`ESC[201~` 整組丟棄**（位元組數正好少 12；win32-input-mode 序列同樣被吃），claude 收不到 bracketed paste 標記、只能靠「輸入叢發時序」猜；而 conhost 轉譯分塊時序不穩（第一行永遠先單獨到、其餘隔 10~175ms、14KB 拆 7 塊）→ 4KB 以上必分裂（碎片留輸入框+「paste again to expand」）、小貼上看時機。正解＝claude 分頁貼上**不走 bracketed paste，改把每個換行送成 `ESC+CR`**（claude 的 Shift+Enter 軟換行鍵，實測可完整穿透 ConPTY、200 行不裂不誤送）：`n` 協定第三欄 flags `c`（AddTab `claudePaste`；Kind=Claude 自動、Custom/PowerShell 依 `IsClaudeExe` 檔名含 claude），JS `doPaste` 統一入口（`v` 協定與 pane 上 capture 階段攔截的原生 Ctrl+V 都走它）。Win11 conhost 若不吃標記也不受影響（claude 分頁一律 ESC+CR，跨版本行為一致）。測試工具在 scratchpad `pastetest/`（pastehost+stdindump，防毒會擋 Start-Process → 用 .NET Process API + CreateNoWindow）。2026-08-15 進階版在 scratchpad `imetest/`：`imehost`（腳本化逐步寫入＋AllocConsole/SetStdHandle 修正＋CLAUDE\*/ANTHROPIC\* 環境清理，可直接跑 claude.exe 互動模式）＋`conread`（ReadConsoleInputW 逐筆記錄 INPUT_RECORD＋libuv 取字規則模擬）。
- **注音組字閃英文字（v1.0.8 修、v1.0.22 改良，只動顯示層）**：微軟注音每鍵先回報原始鍵值（h=ㄏ、8=ㄚ）再更新成注音，xterm 的 `.composition-view` 忠實畫出就閃英數字。v1.0.8 修法「內容一變先藏 30ms＋含字母持續隱藏」有兩個副作用：打字時預覽不斷閃爍消失、英數組字（嘸蝦米/倉頡/拼音、注音內嵌英文）整段看不見（使用者回報「半空格/文字不見」）。v1.0.22 改**依內容判斷**：含 `[A-Za-z0-9]`＝鍵值殘影 → 隱藏、250ms 沒被轉換蓋掉就顯示（＝真的英數內容）；純注音/中文立刻顯示、不再閃。**不碰輸入流**（BEL 那次的教訓）。
- **UI 自動化測試三個必備前置（2026-08-11 各踩一次，都會讓點擊靜靜落在錯的地方）**：
  ① **腳本行程必須先 `SetProcessDPIAware()`**——此機 150% 縮放，DPI-unaware 的 PowerShell 拿到的 `GetWindowRect` 是**邏輯**座標（回報 1295×695），但 `CopyFromScreen`／`SetCursorPos` 走**實體**像素（實際 1942×1042），兩者混用點擊會偏 1.5 倍：實測想點「PowerShell」卻點到第三項「連接埠」開了 COM 對話框。加了之後截圖像素＝視窗相對座標。
  ② **點擊前要驗證視窗真的在前景**（`GetForegroundWindow() == hwnd`，被前景鎖擋住時先 `keybd_event` 敲一下 ALT 再 `SetForegroundWindow`，重試幾次）。`SetForegroundWindow` 失敗是**靜默**的，使用者一切到別的視窗，後續點擊就打在他的視窗上——實測誤點開了「常用字串」對話框、還把主視窗最小化（最小化後 `GetWindowRect` 回 `-21333,-21333`＝150% 縮放後的 `-32000`，記得 `IsIconic` → `ShowWindow(SW_RESTORE)`）。
  ③ **終端機內容要用 `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT=2)` 抓**。WebView2 走 GPU 合成，`CopyFromScreen`／BitBlt 對終端機區域只會拿到**全黑**——別誤判成「分頁沒內容」或「程式壞了」。WPF 外殼（工具列/對話框/分頁列）用一般截圖即可。
  另注意 ContextMenu 是獨立 popup：截圖前若再 `SetForegroundWindow` 會把選單關掉，要用不搶前景的截圖。可重用腳本在 scratchpad `ui.ps1`（shot/shotraw/shotwin/click/clickraw/key/type/windows）。
- **UI 自動化測試的座標會漂移**：`ui.ps1` 用**螢幕絕對座標**，必須每次先截圖取 `GetWindowRect` 原點、再把影像座標加上原點；沿用上一張截圖的座標會誤點到標題列的最小化/關閉鈕（2026-07-30 就這樣誤觸關閉→ExitDialog 被確認→程式結束，一度誤判成防毒問題）。**使用者正在操作電腦時不要跑侵入式點擊**（視窗會被移動/最小化，兩邊互相干擾）。判斷是否為誤觸：`settings.json` 寫入時間若剛好早於程式結束 1 秒＝走了正常關閉流程，不是當機。
- **WebView2 airspace**：WPF 內容**無法可靠疊在 WebView2 上**（複製提示 `CopyPopup` 才用 Popup）。1.0.46 以前的「快速輸入抽屜」因此做在終端機右側欄（黃框外）；現已併入「輸入文字」對話框（獨立視窗，天然沒有 airspace 問題）。
- **COM 輸出慢**：`SerialPort.DataReceived` 有延遲 → 改**專用執行緒 blocking `BaseStream.Read`**（同 ConPTY 讀取迴圈），資料一到立即送畫面。勿改回 DataReceived。
- **WebView2 快取**：改了 `web/` 檔卻「看起來沒變」→ 已用 `WebResourceRequested` 自行伺服＋no-cache 解決；勿移除。
- **Win10 ConPTY 不轉送 alt-screen（2026-08-29 定案）**：子行程／SSH 遠端程式送 `ESC[?1049h`，conhost 自己吃掉、把 alt-screen 內容用 CUP 絕對定位重畫在主畫面（實測送到 xterm 的流裡 1049h=0 次、CUP 597 次）。所以 claude 全螢幕模式（`~/.claude/settings.json` `"tui":"fullscreen"`／`CLAUDE_CODE_NO_FLICKER=1`）在 AwayTerminal 裡＝只剩最後一頁、scrollback 0 行、翻頁只看得到啟動前的內容。claude 自己知道這件事（執行檔字串「fullscreen disabled: Windows over SSH (ConPTY re-rendering) detected」）但只對 claude 跑在 Windows 上的情況自動關，遠端 Mac/Linux 不會。**本機**靠 `App.OnStartup` 的 `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1`（優先於 tui 設定）；**遠端**要在對方 rc 檔 export 或 settings.json 設 `"tui":"default"`（SendEnv 通常被 AcceptEnv 擋）。**claude 2.1.251 會在 `tui` 未設時自動寫入 fullscreen——明確設 default 才不會被翻掉**。使用者回報「只有最後一頁、上一頁是 cd 的地方」就是這個。
- **環境變數**：從 Claude Code 環境啟動會繼承 `NO_COLOR=1`（claude 全無色）等 → `App.OnStartup` 清除並設 `TERM=xterm-256color`、`COLORTERM=truecolor`。另設 `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1`（claude 2.1.x 起 TUI 改用 alternate screen，scrollback 全失效、長回覆只剩最後一頁；此為官方 v2.1.132+ 的 opt-out，改回經典渲染器讓回覆留在 xterm scrollback）。
- **xterm 黑帶**：xterm.css 的 `.xterm-viewport` 預設純黑 → CSS `transparent !important` 蓋掉。
- **xterm `windowsPty` 不可加**：會造成 claude 輸入列殘字（此機 Win10 19045 ConPTY）。勿加回。
- **BEL→綠燈機制別加回**：曾造成卡橘燈與中文回顯破綻（注音「分段多空格」其實是它插隊，非輸入問題）。
- **關閉慢**：WPF 關 WebView2 teardown 慢 → `Environment.Exit(0)`；勿改回 `Close()`。
- **fit 截行**：padding 放在 **xterm 元素本身**（fit addon 會扣除）；放父層最後一行被截 1/3。
- **初始尺寸**：新 session 用 `_lastCols/_lastRows`，勿寫死 80×24。
- **控制字元常值**：`US = ''`、DEL 用 `''`——勿貼不可見原始字元（CS1011）。
- **WinForms**（ColorDialog/FolderBrowserDialog 用）：csproj `Using Remove` System.Windows.Forms/System.Drawing，該命名空間型別一律全名呼叫。
- **WinForms 對話框一定要傳 owner（v1.0.16 修）**：使用者回報「執行 ClaudeCode 有時沒反應、沒跳出資料夾選擇」。根因＝`ShowDialog()` 未指定擁有者時，WinForms 以 `GetActiveWindow()` 猜，在 WPF 程式裡可能猜不到主視窗，**對話框就開在主視窗後面**——現象正是「點了沒反應」。從 ContextMenu（New 下拉）觸發時特別容易中，因為選單剛關閉的瞬間作用中視窗未必是主視窗。解法＝`Services/Win32Owner.cs` 包成 `IWin32Window`，一律 `ShowDialog(Win32Owner.Of(this))`。**新增任何 WinForms 對話框都要照做**（目前 `PickWorkDir`、SettingsDialog 的 adb 瀏覽與色彩選擇皆已修）。**v1.0.16 後仍偶發「沒蹦出來」的殘因**：① `Directory.Exists(LastDir)` 在休眠硬碟/斷線網路磁碟上卡 UI 數秒，對話框遲遲不開 → 丟背景執行緒最多等 300ms（v1.0.22）；② v1.0.22 曾同時加 `Dispatcher.Invoke(空, ContextIdle)` 想等選單收合——**這是反修（v1.0.24 移除）**：idle 類優先權沒有完成時間上限，使用者見沒反應而晃滑鼠/連點時佇列永不閒、Invoke 永不返回＝對話框永遠不開，且 UI 其餘照常運作、事後採證不到；自動化測試點完就靜止不動，所以 30/30 全過也抓不到。**教訓：UI 執行緒上絕不要同步等待 ContextIdle/ApplicationIdle 類優先權**；owned 對話框 z 順序本有保障，開之前 `Activate()` 即可。PickWorkDir 各步驟已留 `Diag.Log` 時間戳（diag.log），再發生看 log 定位——**而 diag.log 上線後第一筆現場（2026-08-11）就抓到真兇＝③ `!_webReady` 靜默吞點擊**（啟動後頭幾秒點 New→…，WebView2 還沒 ready），v1.0.25 改 `DeferUntilWebReady` 排隊補執行。教訓：**守門式 `if (!ready) return` 絕不可無聲返回**——要嘛排隊、要嘛給回饋，否則就是下一個「有時候點了沒反應」。
- **清除畫面**（PowerShell/SSH）：Esc → 延遲 ~60ms → Ctrl+L；黏著送會被 PSReadLine 當 escape 序列。**v1.0.27 起一律先跳 Yes/No 確認**（`msg.clearConfirm`，使用者指定「一律先問」）：兩條路的代價不同——PowerShell/SSH 只是叫 shell 重畫、xterm scrollback 還在（用「翻頁」捲得回去），但 Telnet/COM 走 `term.clear()` 會**把 scrollback 整個洗掉、救不回來**。確認框期間可能被切分頁，所以按下當下先把 `_active` 存進區域變數，之後一律作用在那一頁。
- **DPI 髮絲線**：Window `UseLayoutRounding` + `SnapsToDevicePixels` 已開。
- **深色對話框的 ComboBox 灰字**：視窗層級的灰字 `TextBlock` 隱式樣式會**滲入 ComboBox 模板**（TextBlock 非 Control、隱式樣式穿越 template 邊界），下拉白底配灰字看不清。解法=在 ComboBox 隱式樣式的 `Style.Resources` 放一個黑字 TextBlock 樣式蓋掉（ConnectDialog/ComDialog 已加）；新增深色對話框時記得照做。
- **防毒**：PC-cillin 誤判（ConPTY 開 shell）；專案資料夾已加例外。
- **`.ps1` 有中文必須 UTF-8 BOM**；`.iss` 有中文亦需 UTF-8 BOM（故 installer.iss **一律用英文**，檔頭已註明）；`.reg` 有中文必須 UTF-16 LE。**2026-08-04 又踩一次**：在 installer.iss 加了中文註解後用 `Set-Content -Encoding utf8` 改版號，註解變亂碼並與程式行黏在一起（`Source:` 被吃進註解、`function` 宣告被吃掉），ISCC 報 line 106 錯誤。改動 .iss 請用 Edit 工具逐段改，勿整檔重新編碼。
- **`Get-AuthenticodeSignature` 回 `UnknownError` 不一定是壞事**：訊息若為「terminated in a root certificate which is not trusted」，代表自簽根憑證不在信任存放區——**1.0.12 起安裝檔會主動移除機器層級根憑證，所以這是預期結果**，程式照樣能執行。別誤判成防毒攔截或簽章失敗（2026-08-04 一度誤判）。
- **`[Diagnostics.Process]::Start` 啟動剛建置／剛安裝的 exe 會回 "Access is denied" ——這是 PC-cillin 擋的**（使用者確認）。同一支程式用 Bash 工具 `./AwayTerminal.exe &` 或 `cmd /c` 卻能啟動，所以**別把它誤判成程式壞掉或簽章問題**；驗證時改用 Bash 啟動即可。（2026-08-04 一度誤記為「工具環境怪癖」，已更正。）
- **憑證私鑰不進 git**（在 Windows 憑證存放區）；`settings.json` 在 `%LOCALAPPDATA%`（不在專案內）；`.gitignore` 排除 `bin/ obj/ installer/dist/ installer/MicrosoftEdgeWebview2Setup.exe`。
- **安裝版舊 exe 與開發版共用 settings.json 會「剝欄位」**：Program Files 的 v0.9.73 安裝版一存檔就把它不認識的新欄位（遠端 token/chatId/RemoteEnabled）整組洗掉——2026-07-27 中招，遠端靜默 3 小時才發現（診斷法：對 bot token 打 `getUpdates`，409=有人在 poll、200=服務沒起來）。對策（v0.9.82 起）：`AppSettings` 加 `[JsonExtensionData] ExtraFields` 保留未知欄位、`Save()` 改 tmp+`File.Move` 原子替換（防強殺留半截檔）、`Load()` 解析失敗先備份 `settings.json.bad` 再退預設。**在裝過安裝版的機器測新功能前，先確認沒誤開 Program Files 的舊版**；新功能穩定後盡快重出安裝檔。
- **遠端 /last 絕不能用「原始位元組流去 ANSI」**：claude 等 TUI 用游標定位原地重繪，去 ANSI 直接串接會把幾百次重繪黏成 `Inferring…Inferring…` 洪流；逐格差分重繪還會產生 `oing`/`✣ Bi` 碎片。正解=向 xterm 查渲染後文字（`q…text`→`a…text`，JS `lastPlainText()` 接回 isWrapped 邏輯行），xterm 已把重繪合成完畢。位元組流緩衝（`TerminalTab.RemoteRecent`）僅當 JS 逾時未回的備援。
- **claude inline 渲染器會在 scrollback 留孤兒行**（spinner 片段 `illo`/`· Bill`、歡迎框帶標題長行）——用 `TelegramRemote.NoiseLineRes` 規則逐行過濾；歡迎框上緣「邊線+標題」合併行目前仍會漏掉（待辦）。
- **Telegram bot 測試可全自動**：電腦版 Telegram 用 `tg://resolve?domain=<bot>` 直開對話；打字必用 **Set-Clipboard+Ctrl+V**（SendKeys 會被注音 IME 吃掉）、勿送 ESC（無組字時會關聊天室）；`SetForegroundWindow` 被前景鎖擋時先 `keybd_event` 按一下 ALT。

## 待辦（需使用者輸入）
0. ~~此機 PowerShell 分頁一開就死~~ **已解（2026-08-15，v1.0.28）**：真兇不是防毒——是從 Claude Code 工具環境啟動 AwayTerminal 時繼承的 pipe std handle 被帶給 ConPTY 子行程（見踩雷「std handle 值會傳播」）。`App.OnStartup` 歸零 std handle 後，工具環境啟動的 dev 版 claude/PowerShell 分頁實測正常；使用者桌面正常啟動本來就不受影響。
1. COM「清畫面時送 reset」要送什麼（`SerialSession.SendReset()` 仍為空）。
2. ~~巨集 `elseif`/`break`/`continue`~~ **已做（v0.9.81）**：if..then/elseif/else/endif 分支鏈（`_branchNext`/`_toEndif`）＋ break/continue（`_loopOwner` 取最近一層 for/while；只支援獨立成行，單行式 `if x break` 不支援）。檔案操作/檔案傳輸仍未做，依實際 `.ttl` 需求再擴充。

## 待辦（遠端 Telegram，接續）
1. ~~過濾殘留~~ **已驗收完成（2026-07-27 情境1 tt1 網頁貪食蛇 + 情境2 tt2 C++ 貪食蛇+MSVC 編譯，端到端全通過）**：主回覆完全乾淨。過濾手段=`NoiseLineRes` 規則行過濾（spinner/狀態列/方框/碎片）+ **前綴去重**（空白正規化後某行是較長行前綴 = TUI 截斷殘影）。若日後 claude 換 spinner 字形/statusline 格式再漏，補 `TelegramRemote.NoiseLineRes` 即可。
2. 手機端 adb 截圖驗證：手機有指紋鎖，adb 無法解鎖，**待使用者解鎖後補驗**（`adb shell screencap -p /sdcard/x.png` + pull；`exec-out >` 會被 PowerShell 弄壞二進位。註：1.0.13 起已不內建 adb，測試請用系統上的 platform-tools）。
3. ~~`/new` / `setMyCommands` / `/shot`~~ **已完成並實測（2026-07-27）**：`/new` v0.9.77 改版=**每種連線只列一個**（不列 History；PowerShell/PickDir 自訂以桌面為工作目錄、不跳資料夾框）、開完自動附著、非 SSH 且 follow 開時 1.5s 後自動推開場畫面；`setMyCommands`=服務啟動時自動註冊（手機出現 Menu 鈕）；`/shot`=WebView2 `CapturePreviewAsync` PNG + sendPhoto（附著分頁未顯示會先切前景）。
3a. **SSH/Telnet 遠端登入（v0.9.77~78）**：`/new` 選 SSH → 開「login as:」分頁並提示回覆帳號；`SendInputToTab` 在 `Session==null && LoginBuffer!=null` 時轉 `HandleLoginInput`（修掉遠端打不進 login as: 的問題），密碼走一般文字輸入（送出後 0.7s 自動回傳畫面）。Telnet 直接開，登入提示由開場畫面推播帶出。0.9.78 加專屬指令 `/ssh [user@]主機[:埠]`、`/telnet [主機[:埠]]`（`ParseHostPort` 解析；不帶參數用 `LastHost`＋`LastSshPort`/`LastTelnetPort`；ssh 帶 user@ 走直啟路徑 `IRemoteHost.OpenSsh`、跳過 login as:）。**尚未實機驗**（需使用者手機測 SSH 帳密流程）。
4. ~~`/close`、`/more`、10 分鐘閒置自動 `/exit`~~ **已做（v0.9.81，未實測）**。規格內尚未做：`/clear`、選配 `/arm` PIN、非附著分頁選擇題通知帶內容。
5. 驗完 commit（v0.9.74~82 遠端功能一整包，含 `q…text` 協定）。
6. **2026-07-27 自動化測試已過**：MacroRunner 單元測試 18/18（elseif/break/continue/巢狀/回歸＋ParseHostPort/MenuOptions/TidyForPhone，散彈槍在 scratchpad mrtest 專案）；Telnet 自動重連 e2e（本機假伺服器，斷線後 3s→6s 退避重連實測）；Telegram e2e（/help、/new inline 按鈕點擊開 PowerShell(桌面)+子行程驗證、文字指令回傳、/history 按鈕、/close 確認按鈕→實關）；UI 截圖驗證（右鍵五項選單、Ctrl+F 搜尋 1/2→2/2→Esc）。**未測**：SSH 帳密實機（需真帳密）、/more 長輸出翻頁、COM 重連（無裝置）、10 分鐘閒置（太久）、選擇題按鈕（需 claude 跳選單）。
